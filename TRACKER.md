# GHOSTFACE — progress tracker

Working board for the whole project: business tasks (GF-xx), crypto audit,
release gates, engineering items. **Agents update this file as part of
completing work** — move rows, don't let it rot. STATUS.md holds narrative
session state; this holds the task board. Human-priority column is Benji's
to set.

Status legend: ✅ done · 🔄 in progress · ⏸ blocked/waiting · ⬜ not started

Last updated: 2026-08-28 (Claude Code — devtest merged into feat/push-notifications. FROM devtest: sign-up page redesign + a real recovery-PIN second factor, per-category locks, E2E profile photos, token renames (CASPER→GFC, FaceZero→FANTASMA/FTSM), keyboard fixes and the lock-screen work. FROM feat/push-notifications: the auth-posture pass, all auth-helper consolidation into one `lib/auth.ts`, the failed-recovery-destroys-identity fix, the EAS cloud-build fixes, store metadata moved off the unowned ghostface.app domain, company mailboxes + SPF/DKIM/DMARC, and the /i/ invite landing page. Both branch summaries are preserved below.)

<details><summary>Previous branch summaries</summary>

- feat/push-notifications: 2026-08-27 (Claude Code — auth-posture pass on the three unauthenticated endpoints: client half written (`19db682`), server half DEPLOYED behind `ENFORCE_ENDPOINT_AUTH` which ships OFF (`df0d4bc`) — production behaviour unchanged, and the flag must not be flipped until an app release carrying the client half is out. Then all auth helper duplication removed in three bisectable commits (`7107630` hashToken, `e4b6213` resolvers with a required AliasSource, `1268467` deviceAuthMiddleware); tests 53 → 78; behaviour confirmed identical before/after against live production. Earlier: reconciled this board against STATUS.md — the VPN peer-agent row still read "🔴 OPEN, P0, nothing fixed" while the commits and STATUS recorded it CLOSED on 24 Aug.)
- devtest: 2026-08-27 (Claude Code — sign-up page redesign + a real **recovery-PIN second factor** for restore: 6-digit PIN blinds the identity key before it's encoded as the 24-word phrase (phrase alone is now useless), via NATIVE argon2 (`react-native-argon2`, new dep — pure-JS is ~8s on Hermes); onboarding reordered so the recovery-phrase step is no longer skipped (pre-existing isOnboarded bug), with an interrupted-signup resume path; verified end-to-end on the sim incl. kill-app-mid-flow. Code complete on `devtest`, not yet in a build. Two test identities registered during the walkthrough — re-run the DB prekey completeness query before shipping.)

</details>

## Business / legal (GF tracker)

| ID | Task | Status | Blocked on / next action |
|---|---|---|---|
| GF-01 | Export compliance — engage counsel | ⏳ MATERIALS SENT 25 Aug — clock running; opinion due ~1 Sep; submit held | Enquiry sent from benjamin@ghostface.co.nz with research memo + crypto inventory. Asks US EAR (self-classify vs CCATS) AND NZ strategic-goods (MFAT permit vs Wassenaar mass-market); kdfRkPQ is the crux. **NOT resolved** — awaiting counsel's written answer. CHASE if no reply by ~Mon 25 Aug. App Store submission stays blocked until answered. (Corrects an earlier over-eager "✅ resolved" mark — a sent email is not a legal opinion.) **No reply as at 21 Aug — chase Monday.** ⚠️ **This gate is already pre-answered in the binary:** `app.json` sets `expo.ios.infoPlist.ITSAppUsesNonExemptEncryption: false`, which asserts to Apple that the encryption is *exempt* and suppresses the "Missing Compliance" prompt that would otherwise stop a TestFlight build. It is baked into builds 64 / vc26 (unsubmitted, so nothing has been represented to Apple yet). Note the memo does **not** support that value: it explicitly states it is "not a self-classification decision", and mass-market treatment (5D992.c) is a *classification*, not an exemption — §5 itself asks whether notification/self-classification report obligations survive either way. **Re-decide this flag against counsel's written answer before any `eas submit`.** **22 Aug update:** MinterEllison still silent; chase draft created in Gmail (reply on original thread), one-time reminder scheduled Mon 24 Aug 9am NZT (note: earlier "Mon 25 Aug" was a date error — Monday is the 24th). Parallel outreach sent 22 Aug from benjamin@ghostface.co.nz to two US encryption-export specialists: Olga Torres (olga@torrestradelaw.com, Torres Trade Law) and Lori Scheetz (lscheetz@wiley.law, Wiley Rein) — bounded ask: one-page opinion, §740.17(b)(1) mass-market self-classification vs CCATS re kdfRkPQ, fixed fee requested. Copies were forwarded to jjules@ (Benji's mother — an intended recipient, not a leak). ⚠️ But the two spellings used were `xtra.com` and `xtra.co`, and NZ Xtra addresses are `@xtra.co.nz`; both domains used are real and separately owned, so **confirm she actually received it** — otherwise the mail went to whoever holds jjules@ at those domains. **23 Aug: MinterEllison REPLIED** (sent Fri 21 Aug 7:34pm; landed in the stranded M365 mailbox because MX still pointed at Outlook — MX now fixed to Google, see STATUS): Sarah Salmond (Partner, international trade lead) offers a written opinion covering BOTH questions (US EAR mass-market vs CCATS + NZ strategic goods/MFAT) for **NZ$4,000–7,000 + GST**, ~5 working days from receipt of materials, opinion only (no filings), US co-counsel not anticipated. **25 Aug NZT (24 Aug UTC): ACCEPTANCE SENT** — Benji replied to Sarah accepting scope/fee on standard terms, asked the opinion to also map the conclusion onto Apple's `ITSAppUsesNonExemptEncryption` declaration (or treat as a separate small question), and will return the signed engagement letter + NZBN on receipt. **25 Aug: ENGAGEMENT CONFIRMED** — Sarah advises no separate engagement letter is needed (the acceptance email suffices, standard terms apply). She offers the Apple-flag section (`ITSAppUsesNonExemptEncryption` mapping + questionnaire implications) for **+$500 + GST** on top of the $4,000–7,000, and requests: (a) copies/screenshots of the export-compliance declarations/questionnaire responses previously submitted to Apple for the two TestFlight distributions, and (b) confirmation of the current `ITSAppUsesNonExemptEncryption` value. Current value confirmed from the repo: **`false`** in `app.json`, baked into every build produced to date. ⚠️ 25 Aug correction: 62/64/66 are unsubmitted, but **build 63 WAS uploaded to ASC on 21 Aug** — see the correction row below. **25 Aug (later): DIY path considered and declined.** Weighed answering Apple's questionnaire ourselves against the engagement: Apple's flow consumes BIS paperwork rather than producing a classification (its "key value" is the code the July 90592 rejection was about), and the whole DIY bet reduces to self-answering the one kdfRkPQ mass-market question the memo declined to answer — post-notice, with the NZ half having no DIY path at all. Proceeding with counsel. **Post-opinion plist mechanics (mapped 25 Aug):** the flag change is not just `false`→`true`. Apple's `ITSEncryptionExportComplianceCode` key must accompany `ITSAppUsesNonExemptEncryption: true` **only when Apple required documentation review** — its value is the code Apple issues after approving uploaded documentation (ERN/CCATS-class cases). The mass-market/standard-algorithms path typically requires no doc upload and no code — just the truthful questionnaire/plist answers plus the annual BIS self-classification report filed on our side. July's 90592 was exactly this pairing broken: documentation + code existed in ASC, plist carried neither key. **If Apple still shows the July documentation approved, its key value sits next to it in the App Encryption Documentation section** — worth capturing with the evidence. Both keys go in `app.json` under `expo.ios.infoPlist`.  **25 Aug (evening): MATERIALS SENT.** Benji emailed Sarah the full pack: covering email (final version, five-step evidenced sequence), `GHOSTFACE_Cryptographic_Inventory.pdf`, `GHOSTFACE_Classification_Memo.pdf`, and `GHOSTFACE_ASC_Declaration_Exhibits.pdf` (7-page captioned bundle built from tonight's ASC/EAS screenshots — all three in `compliance/`). The +$500 Apple-flag section was accepted in the same email. **Five-working-day clock is running — opinion expected ~Mon 1 Sep 2026.** While waiting: no uploads, no submissions, no questionnaire answers, no documentation attempts in ASC. On receipt of the opinion: re-decide `ITSAppUsesNonExemptEncryption` (+ possible `ITSEncryptionExportComplianceCode`), make whichever BIS filing it prescribes, build 67 if the flag changes, then submit. Also then: Torres/Wiley stand-down notes if they have replied. **25 Aug: DECISION — no `eas submit` until the opinion lands.** Considered submitting build 66 "to see if Apple pushes it through" and rejected it: the `false` flag suppresses Apple's compliance check, so approval proves nothing, while the submission itself is the first formal exemption representation to Apple — made days before counsel's answer on that exact flag, and while the memo declines to support the assertion. Waiting costs ~a week; build 66 sits ready either way (confirmed flag → submit 66 as-is; corrected flag → one-line change, build 67). **Covering email drafted: `compliance/gf01-materials-covering-email.md`** — final version incl. the three-upload history; the inventory + memo PDFs are in `compliance/` in this repo (earlier note saying claude.ai-only was stale). **Next actions: (1) Benji sending materials (inventory + memo + ASC screenshots) — accepts the +$500 in the same reply; 5-working-day clock starts on receipt; (2) on the opinion: re-decide the flag, then submit; (3) Torres/Wiley — courtesy stand-down notes if/when they reply.** **26 Aug: RECEIPT CONFIRMED.** Sarah acknowledged the materials — "we'll review the materials and come back to you if we have any questions… we expect to provide our advice early next week." So the ~1 Sep clock is intact. **Chase pinned Wed 3 Sep:** if no written opinion by then, draft a polite status-check reply on the existing MinterEllison thread (review before sending). (A one-shot session reminder was set for that morning too.) | **27 Aug: territory-scope question added to the thread** — the original enquiry covered classification and the Apple declaration but not distribution geography, and Benji intends worldwide App Store availability. Asked which destinations must be excluded regardless of classification, which territories require registration or a local entity for encrypted messaging/VoIP, and whether mass-market treatment changes either. Framed to fold into the existing scope or be quoted separately so it does not reset the five-working-day clock. Text at `compliance/gf01-territory-followup.md`. **The 1 Sep / chase 3 Sep dates are unchanged.** **28 Aug: Sarah replied on the territory questions — all three are OUT OF SCOPE.** She declines Q1 (which destinations must be excluded as a matter of law) and Q2 (registration / local entity / licensing) outright: both need jurisdiction-specific advice across telecommunications, cybersecurity, privacy, sanctions and export controls, and as NZ lawyers MinterEllison are not qualified to advise on all of them. **Q3 she does answer, and it is the important one: mass-market treatment does NOT change either answer** — it governs export-control classification and the availability of exemption pathways only, and does not determine whether local telecoms, licensing, registration, cybersecurity or privacy requirements apply in a given market. Even if the app qualifies as mass-market, separate local requirements may still bite. The opinion stays scoped to the two export-control questions plus the Apple flag section, and **the ~1 Sep timing is unaffected**. ⚠️ **This reply is NOT the opinion** — GF-01 remains open, `ITSAppUsesNonExemptEncryption` stays undecided and `eas submit` stays blocked. **Consequence: worldwide distribution has no legal owner.** Nobody is advising on which markets are lawful, and mass-market treatment will not fill that gap. Decision taken and put on the record with counsel (reply sent 28 Aug, Gmail id `1a04809c6ab099e2`, Sian Vaughan-Jones cc'd): **launch with App Store availability limited to NZ / AU / UK / US** — the same four the API already knows via `COUNTRY_NAMES` in the numbers route — and take local advice before opening any further market.
| GF-02 | Crypto inventory | 🔄 evidence located 25 Aug | **Full July picture recovered 25 Aug from the EAS submit log.** THREE upload attempts exist, not two: (1) **Jul 3** — v1.0.0 (build 27), submitted by an automated "Submit to TestFlight" workflow from a DIFFERENT EAS project, **"MAYBACH MONEY"** (`0b6b78a6-…`, vs today's `mayybachh` `1955ab0c-…`; Expo account display name since renamed to "B Henderson", 25 Aug — the Jul 3 log still shows the old name), rejected mid-upload with Apple error **90592**: "export compliance key value **[]** in the Info.plist doesn't match the app's export compliance documentation" — i.e. Benji's manual questionnaire had created **export-compliance documentation in ASC with a code value** (the honest non-exempt declaration) and the binary carried an EMPTY plist key, not `false`. (2) **Jul 16** — v1.0.1 (build 27), reached ASC, Invalid Binary. (3) **21 Aug** — build 63, `false` in plist, Validated, internal-testing-to-self only. **So the honest documentation predates the `false` flag entirely.** Open question for the API pull: how did build 63 validate with `false` if non-exempt documentation was on file — superseded, expired, or invalidated? `appEncryptionDeclarationState` will say. The July documentation record (with `codeValue`) still exists in ASC and is retrievable — exactly what counsel asked for. Those July questionnaire responses are exactly what Sarah asked for: capture them from ASC if the UI still shows them (encryption/compliance section, or via the `appEncryptionDeclarations` API — Issuer ID errand revives if the UI won't show history). **Check before sending materials: TestFlight tab → do any builds show Testing/Expired (i.e. actually reached testers), or are all Invalid?** Capture the full build-detail page per build (upload date, encryption line, binary state) — this satisfies Sarah's request (a); the ASC-API pull is now optional. Then send screenshots with the inventory + memo. |
| GF-03 | GHOSTFACE word mark (IPONZ) | ✅ 11 Aug 2026 | Filed under Ghostface Limited, classes 9/38/42. |
| — | Hood-logo figurative mark | ⏸ waiting | File once figurative clearance opinion obtained. |
| — | CASPER wallet mark | ✅ retired 26 Aug (renamed to GFC) | **CASPER dropped in favour of GHOSTFACE COIN (GFC)** — the Casper Network (CSPR) s 25 conflict is now avoided by not using the name at all; no CASPER mark to file. Rename done in code 26 Aug (commit a21f153): server seed + full app rename. ✅ **26 Aug: prod DB updated** — ran `UPDATE tokens SET name='GHOSTFACE COIN', symbol='GFC', logo_color='#E7C765'` on Railway Postgres (row id 1, was CASPER, `pending`/no-mint so no on-chain impact). Verified live: `GET /api/tokens` now returns GHOSTFACE COIN/GFC. Code + DB + API consistent. Protect GFC per the token-marks row below. |
| — | FANTASMA + GFC (GHOSTFACE COIN) token marks (IPONZ) | ⬜ not started | Chosen 23 Aug as the token names to protect (GHOST COIN rejected — ≥4 existing "Ghost" crypto projects incl. a Ghost token on Solana; CASPER conflicted, see row above). File under Trade Marks Act 2002: application s 32, registrability s 13, distinctiveness s 18, prior-mark conflicts s 25; 10-year renewable term ss 57–58. Classes: 9 (downloadable tokens/software), 36 (financial/monetary + virtual-currency services), 42 (software services) — class 36 is the critical one for tradeable tokens. Owner: Ghostface Limited, same as GHOSTFACE word mark (GF-03). GFC benefits from the existing GHOSTFACE registration (brand family). Sequencing: clearance search both names first; same FMA-advice gate as CASPER applies before any token mark is used in trade. Also consider GHOSTPAD mark (classes 9/38/42) in the same filing batch. |
| GF-07 | iOS production build (build 72) | 🔄 built, not submitted | Production build **62** completed 19 Aug (`b5342936-dcef-4755-9f16-da79d96a58c6`, buildNumber auto-incremented 61→62), run on Benji's explicit override of the still-open CSPRNG/TestFlight gates below — those gates are **not** cleared, only bypassed for this build. Artifact is the raw signed `.ipa` (`https://expo.dev/artifacts/eas/RRlfJZFtmjiTtugvfY4cVJUD7FW4gNkc34PgVyWOBIM.ipa`), store-distribution — **not directly installable**; reaching TestFlight/App Store needs `eas submit`, which stays off-limits without a separate explicit go-ahead. **Never run `eas build`/`eas submit` without explicit go-ahead.** |
| — | Business address / DUNS record update | 🔄 contact letter SENT to D&B (26 Aug) | Update the registered business address on file for **GHOSTFACE LIMITED** to the register address **231 Toppings Road, Canterbury 7477, New Zealand**. Reference **DUNS 594353892**. Relevant to organization enrolment / verification (Apple Developer + Google Play cross-check DUNS ↔ NZBN ↔ Companies Office). ✅ **Postcode corrected 26 Aug: 7477, NOT 8000** — the Companies Office register confirms 231 Toppings Road, Canterbury **7477** (earlier '8000/Sefton' was wrong; caught before sending). Director on register: **Benjamin HENDERSON**, residential address same as above, appointed **09 Apr 2026**, shareholder **yes**. Address-update request drafted (aimed at Dun & Bradstreet; retarget to the enrolment/verification team if that's who asked). **Still needed before sending:** add the **NZBN** so DUNS ↔ NZBN ↔ register reconcile. **26 Aug: SENT.** Address-update contact letter sent to Dun & Bradstreet (DUNS 594353892) requesting the record be updated to 231 Toppings Road, Canterbury 7477. Awaiting D&B confirmation. If they ask for the NZBN or a Companies Office extract as evidence, provide it. |
| GF-12 | Production builds 64 (iOS) / vc26 (Android) — glass restyle | 🔄 built, not submitted | Run 21 Aug on Benji's explicit go-ahead, with the CSPRNG/TestFlight gates below still **open, not cleared** (same override posture as build 62) and GF-01 export compliance still unresolved. iOS: v1.0.2 buildNumber 63→64, build `848be4e7-8ca1-46ec-8824-b127780b3c8b`, `.ipa` `https://expo.dev/artifacts/eas/RFUURzCpQMibLpEauR7ISIjDYnaOQuve_bdgO3gRync.ipa` — store-distribution, **not directly installable**. Android: versionCode 25→26, app-bundle, default keystore `TVxbfrwdDe`, build `f379111b-4568-45b7-803a-4bca57519b62`, `.aab` `https://expo.dev/artifacts/eas/B11DqiMccR_bfF1tkwLx-_T4HvhTxWBAk50BaYKZoW4.aab`. Contains the app-wide glass button restyle, new chat palette + `getContrastText`, solid-colour wallpaper scrim, and the `WIDE_SURFACE_SPECULAR` fix (verified in simulator). Does **not** contain fixes for the wallet BUY wrong-asset bug or the placeholder wallet address. `eas submit` not run — needs its own go-ahead. |
| GF-11 | ASC API key regeneration | 🔄 possibly resolved — unconfirmed | Marked "broken" previously, but the 19 Aug production build's credential fetch line read "Using App Store Connect API Key from EAS credentials service" and succeeded without issue. Could mean the key already works for build-time profile fetch but still fails for `eas submit`'s different API scope — don't assume `eas submit` will succeed until actually tried. |
| GF-13 | iOS production build **66** | 🔄 built, not submitted | Run 25 Aug on Benji's explicit go-ahead (same override posture as builds 62/64 — CSPRNG/TestFlight gates still open; GF-01 now moving: MinterEllison engagement being accepted). v1.0.2, buildNumber 65→66 (65 was burned by a failed attempt, see below), build `5fab5058-bb32-4ffc-b8a3-65c869253c0b`, `.ipa` `https://expo.dev/artifacts/eas/zwN26J4NVuh0z-zDH5vIGAXAXfe0e7Yj1GzQ-kJ2pbU.ipa` — store-distribution, **not directly installable**; `eas submit` needs its own go-ahead. **Built from commit `806ab1f` via a throwaway worktree, NOT branch head** — head carries the WireGuard WIP (`@bacons/apple-targets` scaffolds an extension importing WireGuardKit that nothing links on EAS), so a head build would fail. Contains everything through the onboarding recovery-phrase copy fix; does NOT contain the WIP. ⚠️ **First attempt (`8add67ad`, burned #65) failed on signing:** the committed `com.apple.security.application-groups` entitlement (added for the tunnel extension) is not in the 13-Aug provisioning profile — **every production build from the branch will fail the same way** until the profile is regenerated with App Groups (interactive `eas credentials` / non-`--non-interactive` build; needed anyway before the extension ships). Build 66 succeeded because the entitlement was stripped in the worktree only. **RESOLVED 25 Aug:** the interactive `eas credentials` run completed (real Apple ID + 2FA; two passes — the first authenticates only when it reaches the credential-less tunnel target, so a second pass is needed for the main target to validate against Apple). Portal capability sync **created and linked `group.com.ghostface.app`** and regenerated both profiles: main `89A3CY9F86` (now includes App Groups — the build-65 failure cause) and a new tunnel profile `76QM629KQ5` for `com.ghostface.app.tunnel` (previously "No credentials set up yet"). "All credentials are ready to build" for both targets — **the branch can now produce store builds as committed.** ⚠️ **Side effect to remember for the WireGuard work:** capability sync mirrors *declared entitlements*, and the current app.json declares only app-groups — so the sync **DISABLED Network Extensions on `com.ghostface.app` and Personal VPN on the tunnel App ID**, partially unwinding the 23-Aug portal setup. When the VPN client resumes, add `com.apple.developer.networking.networkextension` to both targets' entitlements, re-run credentials (capabilities re-sync, profiles regenerate again) — otherwise that build will fail with a signing error that will look mysterious. Lessons encoded in `artifacts/ghostface/CLAUDE.md`: the Apple ID prompt is a text field; use `EXPO_NO_KEYCHAIN=1` to dodge the junk `n`/`y` keychain entries. |
| GF-14 | iOS **dev-client** builds, buildNumber 66 (28 Aug) | 🔄 **built + distributed internally; NOT on the tracker until now** | Five `development:device` builds ran 28 Aug, all FINISHED, all `distribution: INTERNAL`, v1.0.2 buildNumber **66**: `a14321a1` 06:58 UTC (commit `88dbe34`), then `6f8d9eea` 08:07, `ea41c94e` 08:16, `84e9f13d` 08:31 and **`e5111cf1` 08:46 — the latest** (all four from commit `f1df8a0`). None was recorded here at the time; found 29 Aug via `eas build:list`. ⚠️ **BUILD NUMBER COLLISION — read this before citing "build 66".** Production build 66 (25 Aug, EAS `5fab5058`, `distribution: store`, built from `806ab1f` in a throwaway worktree — see GF-13) and these five dev-client builds are **different artifacts sharing buildNumber 66**. They are not interchangeable: GF-13's 66 is store-distribution and not directly installable; GF-14's 66 are internal and install straight from the build URL onto registered devices. **Contents:** `f1df8a0` is an ancestor of HEAD, 14 commits back, so these carry the JS/visual work that the dev client could not show on device (coin motion, halo fixes, Pig-Latin removal, English-only language list, and the `PREVIEW_STEP` onboarding fix) — but **NOT** the delivery-ack fix (`11f5c3a`), the panic rim (`3786f1c`), the Network Extension / Personal VPN entitlements, or the endpoint-auth client half. **They predate `b2484ce`**, which is why they signed cleanly — the entitlement that broke signing landed after them. ⚠️ **Do NOT install these over a device holding a production VoIP token.** They are dev-client builds on sandbox APNs; per the VoIP row that clears the token and stops CallKit ringing, which is what happened to the Air. ❓ **Unconfirmed: what "uploaded" means here.** The EAS records show internal ad-hoc distribution, not a TestFlight submission. If `eas submit` was in fact run for any build 66, that is a **third upload to App Store Connect** and GF-01's evidence set for counsel is incomplete — the ⚠️ CORRECTION row below documents builds 63 only. Confirm before the ~1 Sep opinion lands. |
| ⚠️ CORRECTION: build 63 WAS uploaded to App Store Connect (21 Aug 00:30 UTC) | ✅ DISCLOSED 25 Aug — no further action |  Found 25 Aug while gathering Sarah's evidence. The tracker's standing claim that no build was ever submitted ("nothing has been represented to Apple") is **wrong**: production build 63 (v1.0.2, commit `ca2d7aad`, actor `ghost_face`, EAS build `fc0b9400`) was built 20 Aug 23:51 UTC and uploaded to ASC ~00:30 UTC 21 Aug — **~20 hours after the enquiry email told MinterEllison distribution was paused**. ASC recorded **`App Uses Non-Exempt Encryption: No`** and the binary is **Validated** (contrast the Jul 16 upload, Invalid Binary). **TestFlight status confirmed 25 Aug:** build 63 was released to **internal testing only, with exactly two tester accounts — both Benji's own Apple IDs** ("myself and myself"). No external testers, no public link, no third party ever received the binary. So the accurate statement is: one validated upload exists post-enquiry, available only to the developer's own accounts — distribution to any third party remained paused throughout. Evidence set for Sarah is now: Jul 16 upload (invalid, declaration recorded) + 21 Aug upload (validated, declaration recorded) + current flag value. **The covering email must state the 21 Aug upload and its timing accurately** — counsel needs the true sequence, and if no testers ever had access, say that too. Post-opinion decision: whether to let 63 expire in TestFlight (uploads auto-expire after 90 days) rather than ever distributing it. | **✅ 27 Aug correction: this was already disclosed.** Point 5 of `compliance/gf01-materials-covering-email.md` (sent 25 Aug with the materials) states the build 63 upload, that it occurred approximately 20 hours after the initial enquiry, the recorded "App Uses Non-Exempt Encryption: No", and that distribution has only ever been TestFlight-internal to two of Benji's own Apple IDs. This row sat at ⬜ for two days after the fact — a session reading it would have re-disclosed something counsel already has. |
| — | Vonage: SMS API + virtual numbers | ⏳ **reply SENT 27 Aug**, awaiting answer | Aaron Lee (SDR) confirmed we can start using the SMS API and subscribe virtual numbers, subject to per-country regulations. Reply drafted at `compliance/vonage-reply-draft.md` — send from benjamin@ghostface.co.nz on the existing thread. It deliberately says **nothing about the app's cryptography** (GF-01 is still with counsel; if Vonage sends a questionnaire touching encryption, check it against the classification memo before answering). ⚠️ **Question 1 is the one that matters:** assigning carrier numbers to individual end users and rotating them automatically is close to sub-allocation/resale, which many providers restrict, and several jurisdictions require the **end user's** verified identity and address to hold a number — awkward for an alias-only, no-PII product. `lib/rotationScheduler.ts` already does exactly this pattern, so a "no" from Vonage means GhostNumber needs rethinking before launch. Get it in writing while it is still a sales conversation. | **Sent 27 Aug** from benjamin@ghostface.co.nz (Gmail id `1a046911af7a4b96`) as a new thread — Aaron's original is not in that mailbox, since prior contact was from Benji's personal mayyybachhh@icloud.com; the email opens by noting the address change. **28 Aug: Aaron REPLIED, and FOLLOW-UP SENT** (Gmail id `1a04801c0d47f909`, on thread `1a046911af7a4b96`; draft kept at `compliance/vonage-followup-draft.md`). What his answers settled: **Q1 is a qualified yes, but possibly to a different question** — he justifies the use case via *masked calling*, where the platform holds the number and proxies two parties without revealing either. GhostNumber assigns each end user their own persistent number, and the words *sub-allocation* and *resale* — the framing flagged in this row as the actual risk — appear nowhere in his answer. Re-asked in those exact terms, and escalated: asked to be routed to someone who can speak for Vonage contractually rather than as sales guidance. **Q2 — end-user KYC is OURS.** We are the data controller and must collect and retain end-user identity/address records, producible on request. That is a structural conflict with an alias-only, no-PII product, not a paperwork task; it may confine GhostNumber to his third tier (any worldwide address), so a country-to-tier mapping was requested. **Q3 — 90-day ageing.** Released numbers do not re-enter the pool for 90 days, so `lib/rotationScheduler.ts` can **never recycle our own inventory**: every rotation permanently consumes new numbers and the released one is still billed for the current month (~double MRC during overlap). This makes the per-user recurring cost on the prize-pool row materially worse than modelled. Also asked for a written carve-out that routine automated per-user rotation is not **snowshoeing**, which his fraud policy prohibits and which our rotation pattern could resemble on the metrics. **Q7 went unanswered** despite being flagged as the one that mattered most (hard exclusions: where assigning a number to an individual end user is not permitted at all, or needs local presence/entity) — re-asked. ⚠️ **New Zealand is absent from his SMS+Voice combined list**, though Q8 says numbers exist for all four first-wave markets; asked whether NZ supports inbound SMS and inbound voice on the same number. **10DLC may not scale:** "each end-customer brand registered separately" does not work when end customers are individual consumers, and the 3-month minimum campaign duration collides with rotation; carrier non-compliance fees start **1 Oct 2026**. ⚠️ **AUP: inbound OTP/2FA on our numbers is blocked at platform level** — users given a private number will try to register WhatsApp/Google/banks with it and those inbound messages are intentionally filtered. That is a product/UI expectation problem to handle before launch, not a legal one. **Convergence with GF-01:** Vonage may answer the *numbering* slice of the territory question MinterEllison declined — Aaron's Q7 answer will be a partial map of where we can operate.
| — | ⚠️ Worldwide App Store distribution — territory scoping | ⏳ asked 27 Aug, awaiting counsel | Benji's stated intent 27 Aug: **every App Store, whole nation**. This is a GF-01 question, not a product setting. Distributing encryption software worldwide is precisely what the US EAR / Wassenaar analysis turns on, and **sanctioned destinations must be excluded as a legal requirement** — Apple's territory selector defaults to all territories, so shipping without deselecting is an affirmative act. Separately, some markets restrict or ban E2EE messaging and VoIP outright, and others require local registration. **Follow-up SENT 27 Aug** on the existing MinterEllison thread (text kept at `compliance/gf01-territory-followup.md`). The enquiry as sent asked about classification, not distribution geography, so her opinion may not cover it unless asked. The draft is framed to fold into the existing scope or be quoted separately, so it does not reset the five-working-day clock on the opinion due ~1 Sep. Do not set App Store availability to worldwide before the written opinion lands. |

## Crypto audit (detail: artifacts/ghostface/docs/AUDIT_FINDINGS.md)

| # | Finding | Status |
|---|---|---|
| 1 | Canonical AEAD associated data (Double Ratchet) | ✅ pushed |
| 2 | *(text never delivered — exists only in Benji's audit conversation)* | ⏸ waiting on Benji |
| 3 | PQ downgrade rejection (REQUIRE_PQ) | ✅ pushed |
| 4 | *(text never delivered — same as #2)* | ⏸ waiting on Benji |
| 5 | CSPRNG chokepoint + boot assert | ✅ pushed |
| 6 | Storage/message AD + migration tiers | ✅ pushed |
| 7 | Stealth AD (GHX3, permanent GHX2 decode) | ✅ pushed |
| 8 | Stealth passphrase required (no default key on encrypt) | ✅ pushed |

## Build-72 pre-ship gates (ALL must pass before any EAS build)

| Gate | Status | Next action |
|---|---|---|
| DB prekey completeness | ✅ **re-passed 27 Aug, test aliases confirmed gone** | **9/9 identities complete** — every one has `ik_sign_public_key`, `spk_signature`, `pqkem_public_key` and `pqkem_signature`, all have a `delivery_id`, and none is short of one-time prekeys (8-10 unconsumed each). The 19 Aug pass was also 9/9 but had gone stale: every row in the table was created 25 Aug or later, so it had been verified against a population that has since turned over entirely. **devtest had flagged two throwaway identities from the recovery-PIN walkthrough — `GSTTEST9` (self-destructed) and `SPECTRE_2` (pending deletion) — as needing confirmation. The re-run answers it: neither appears among the 9, so both are gone from prod.** Re-run again if anyone registers before shipping. |
| CSPRNG on-device smoke test | ⬜ | Physical iOS + Android, clean install, verify clean boot. Plan approved. Benji's devices required. |
| TestFlight coordination | 🔄 | Release note drafted (AD wire break: old↔new builds cannot message; update together). Publish with the build. |

## Engineering — open

| Item | Status | Next action |
|---|---|---|
| Recovery PIN — real second factor for restore | 🔄 27 Aug, code complete on `devtest`, **not yet in a build** | The 24-word recovery phrase IS the raw identity key, so a leaked phrase = full account takeover. Added a mandatory **6-digit recovery PIN** that blinds the key before it's encoded as the phrase: `phraseValue = identityKey XOR argon2id(pin, salt)` — restore needs BOTH phrase and PIN; the phrase alone is useless. argon2 runs **native** (`react-native-argon2 ^4.0.0`, **new dep** → Argon2Swift/argon2kt pods) because pure-JS argon2 under Hermes is ~8s (native ~0.5s sim at 64 MiB/t3); benchmarked + New-Architecture-verified on the sim. Salt is derived from the alias (SHA-256) → **no backend change**, preserves "we never store it". `lib/recoveryPin.ts` (blind/unblind + a local PIN verifier so Settings rejects a mistyped PIN). **Onboarding reordered**: `setAlias` no longer flips `isOnboarded` — it was unmounting `OnboardingScreen` and **skipping the recovery-phrase step entirely** (pre-existing bug: no shipped user had ever seen their phrase). Now the recovery-PIN step precedes registration and `completeOnboarding()` marks done only after the phrase is acknowledged. **Interrupted-signup resume**: a `signupPendingPhrase` flag (AsyncStorage — cleared on reinstall/wipe, so it can't be confused with leftover Keychain keys) makes a killed-mid-signup relaunch resume at a "FINISH SETTING UP" step instead of stranding an unrecoverable identity. Settings "view recovery phrase" now gates on the recovery PIN. **Verified end-to-end on the sim (27 Aug):** recovery-PIN step shows → phrase shows with 2-factor warning → kill-app mid-flow → resumes → PIN validates → identical phrase re-shown; plus an in-app crypto round-trip self-test (correct PIN→exact key, wrong PIN→different key). tsc clean. ⚠️ **Security ceiling:** the prekey-bundle endpoint is unauthenticated, so an alias's public key is fetchable → the PIN is offline-brute-forceable, bounded by 6-digit entropy × argon2 cost (weeks of dedicated compute). A real second factor, not unbreakable — harden later (auth the bundle endpoint, or a server-gated random salt). ⚠️ **Legacy break:** existing raw-key phrases can't restore through the mandatory-PIN path — pre-launch re-register. Needs a **fresh native build** (argon2 autolinks from package.json; `ios/` is gitignored). Commit on `devtest`. |
| Sign-up page redesign | 🔄 27 Aug, code complete on `devtest`, verified on the **sim** (not a physical device) | Onboarding hero reworked. **Coin logo** (`GhostLogo` coin mode): metallic coin with the hooded GHOSTFACE trademark face, a **diamond-cut milled edge** (84 bright/dark facets), thin realistic-silver rim; taps to flip like the home-screen radial coin. **Spin-to-win** welcome gift: every spin lands on one of three prizes (1 mo **Specter** / 1 mo **Phantom** / a **virtual number** 1 mo) — no losing outcome, nothing to pay to play (App-Store-safe framing). Presentation only — **server-side entitlement grant + one-per-account cap still TODO before ship**, and the tier names must match real products. **GHOSTFACE®** wordmark now set in **Cinzel** (`@expo-google-fonts/cinzel`, **new dep**). Removed the GF top badge and the "FIRST LOGIN SPECIAL" banner (deduped vs the spin's Ghost Number prize). White-bordered alias chips. Recovery-phrase step got a proper **red two-factor warning** callout. tsc clean. Commit on `devtest`. |
| Call bug: hangup during ring leaves callee ringing + call bounces back | 🔄 in preview/prod build 62 | Server fix live on Railway (`864a40f`). Re-test surfaced two client bugs: phantom "missed call" logged on the DIALING device (AppContext logged missed for ANY unmatched hangup) and zombie-join (CallKit answer navigates into a dead call for 30s, whose timeout hangup caused that phantom log). Fixed: missed only logged when a real incoming banner existed; new `lib/endedCalls.ts` TTL set marks every received hangup, checked by both answer paths + call.tsx mount. tsc clean, 89/89. Now shipped in preview build (`b2b9372d`) and production build 62 (`b5342936`) — **needs device repro re-test.** |
| Call page: no way to erase history | ✅ 19 Aug, in build 62 | `clearCallHistory()` in AppContext (persists empty encrypted blob, same write path) + trash icon with confirm on the CALL tab header. |
| Chat wallpaper: custom photo option | ✅ 19 Aug, in build 62 | PHOTO tile in the wallpaper sheet — picked image copied to app-private chat-bg/ dir, rendered full-bleed under a 55% black scrim for text legibility; DEFAULT/colour selection cleans up the old file. Image file is app-sandboxed but not encrypted (documented in Conversation.bgImageUri). |
| Disappearing messages: always on, 5s–7d, default 1h | ✅ 19 Aug, in build 62 | The TTL machinery already existed (envelope-carried ttlMs, read-based expiresAt, sweep). Change was policy: OFF option removed, 5s preset added, default 1h; existing conversations migrated on load; peer-synced timer values clamped to [5s, 7d] (old builds' "OFF" becomes 1h). clampDisappearSec in AppContext is the single enforcement point. |
| Local STATUS/TRACKER commits unpushed | ✅ 19 Aug | Pushed — `feat/push-notifications` now in sync with origin through `864a40f`. |
| pg_dumpall incident backups on disk | ✅ 19 Aug | Located and deleted `pre-rotation-dump-20260819.sql` (1.8MB, full cluster dump incl. role password hashes) + its `.stderr` sibling, found in Claude Code session `e968c7a2`'s scratchpad (`/private/tmp/claude-501/.../scratchpad/`). **Only 1 file found, not 2 as STATUS.md's "one per rotation" implied** — exhaustive search of home, /tmp, /var, and all session scratchpads on this Mac turned up nothing else. If a second dump exists it may be on a different machine/session already cleaned up; flag to Benji if that matters. |
| Legacy AD/plaintext tiers in secureStorage | ⏸ scheduled | Remove a release or two after 72 ships; unknown format then becomes a hard error (audit #6 follow-up). |
| Alias-normalization vulnerability (registration/add-contact identity confusion) | ✅ 20 Aug | `normalizeAlias` was stripping instead of rejecting invalid chars, inconsistently between client/server; onboarding never previewed the actual normalized alias before registering. Fixed with a strict reject-based `^[A-Z0-9_]{3,20}$` allowlist everywhere (client keystroke filter + shared normalizer across all api-server routes and ws/manager.ts). tsc clean both sides, vitest 53/1/0, prod check query returned 0 pre-existing violations — no grandfathering needed. |
| Radial menu buttons: remove gold edge | ✅ 20 Aug | Inactive nodeCircleGlassBorder/nodeCircleRim switched from GOLD_OUTLINE_COLOR to a neutral rgba(255,255,255,0.18) rim; active state (white) unchanged. |
| Messages list: remove fake "LIVE" badge | ✅ 20 Aug | Badge was driven by static `isRealContact`, not actual presence — misleading next to the real ONLINE/OFFLINE indicator in chat headers (which is presence-driven and was left untouched). |
| Settings page: style makeover to match rest of app | ✅ 20 Aug | Unified 3 inconsistent status-pill styles into one `StatusPill` (secure/danger/neutral tones), fixed the SUBSCRIPTION card's margin + moved it off inline styles, consolidated repeated inline styles into the shared StyleSheet, applied one label-tinting rule (danger rows only), realigned a few icons to the app's outline/solid convention. |
| App-wide glass button restyle + new chat palette | 🔄 21 Aug, code complete, **not visually verified** | All non-semantic buttons across 16 files converted to `<GoldGradient>` glass (incl. reaction chips, PIN pad, attach sheet, wallet actions, invite/ghostpad panels). Semantic-colour buttons deliberately left flat (red decline/hang-up/destructive, green accept, VPN status toggle, lock duressBar, wallpaper swatches); DELETE SEALED CONVERSATION converted to glass but keeps destructive-red icon/label. `CHAT_COLOR_PALETTE` replaced (was 8 near-black tints) with white/black/grey/blue/red/gold + new `getContrastText()` for avatar initials, and the chat wallpaper scrim now covers solid colours too, not just photos. tsc clean. **Open:** `GoldGradient`'s specular highlight (0.65, tuned for 44×44 radial nodes) washes out labels on wide buttons — default lowered to 0.22 (`WIDE_SURFACE_SPECULAR`) but Fast Refresh never picked it up, so the fix is UNVERIFIED on device. Verify before shipping. |
| WIPE DEVICE button — renamed and restyled | 🔄 **ported onto `feat/push-notifications` 29 Aug; not visually verified** | Two commits were sitting on the `worktree-panic-red-outline` branch in `.claude/worktrees/panic-red-outline/`, unmerged and unrecorded, found 29 Aug while clearing a stray root `app.json`. Cherry-picked clean (no conflicts — `3786f1c` was already shared ancestry, not a divergence) as `b9722be` + `4191a83`. `npx tsc -p tsconfig.json --noEmit` exit 0. **`b9722be` copy:** `SELF DESTRUCT` → `WIPE DEVICE` on the button label and its DEVICE SECURITY row in `security-audit.tsx`. Deliberately narrow — hyphenated *self-destruct* is the unrelated ephemeral-message concept (expiring messages, invite timers) and is untouched; internal identifiers (`PanicButton`, `panicHeld`, `startPanic`, testID `panic-btn`) are also untouched, since renaming those spans `settings.tsx` and `index.tsx`. Held state stays `WIPING...`. No `SELF DESTRUCT` strings remain in `app/` or `components/`. **`4191a83` style:** surface moves from `RED_GLASS_TINT` to the app-standard `GLASS_TINT_BLACK`, and the danger signal moves to the rim, taken from `colors.destructive` at the call site so it follows the theme. ⚠️ **This intentionally supersedes part of `3786f1c`, which is already on the branch.** That commit had just tuned `btnWrap` to a white glass hairline (`rgba(255,255,255,0.32)`) and nudged the red glow to `0.5/16→18`; the port replaces that rim with a red one and **deletes the `boxShadow` entirely** (the `boxShadow` import is gone — only a comment references it). Reasoning in `4191a83`: a glow and a rim in the same colour both outside the glass read as a coloured leak, so the outline is the whole signal. Net effect is a design reversal of `3786f1c`'s visual intent, by a later commit — not a merge accident. **Needs eyes on device:** rim weight and the hold-progress sweep alpha (`rgba(255,255,255,0.22)` → `rgba(255,59,48,0.32)`, the only feedback that the 3-second hold is progressing) both want checking at **`scale={0.5}`, which is what BOTH call sites pass** — `app/(tabs)/settings.tsx:1574` and `app/(tabs)/index.tsx:574`. (`4191a83`'s own commit message says "Settings' scale 0.8"; that is wrong — verified 29 Aug against both call sites. No call site passes 0.8.) Not in any build yet — see GF-14. |
| Panic caption tracking — last unported commit (`a96ccb4`) | ✅ **ported 29 Aug (one hunk, by hand)** | `worktree-panic-glass` is the third and last of the stray worktree branches. Its worktree was never created (branch only), and it holds two commits not on `feat/push-notifications`. One is superseded, one is not. **`39ad293` — already landed by another route.** Its `scale` prop is in HEAD (`scale?: number`, `scale = 1`) and both call sites pass it, so nothing to port. **`a96ccb4` — genuinely missing.** It makes the panic caption's letter-spacing scale with the type: `letterSpacing: TRACKING.label * scale` instead of a fixed value. HEAD does **not** import `TRACKING` at all (verified 29 Aug), so this never arrived. ⚠️ **The bug it fixes is live on both screens.** Tracking stays fixed while `fontSize: 10 * scale` shrinks, so at `scale={0.5}` — which is what *both* call sites pass — the caption's tracking is proportionally twice as loose as intended. Its second change, correcting the `solid` rationale comment to say both call sites sit on flat black, **is** already covered by `4191a83`'s rewritten comment, so only the `letterSpacing` hunk is worth taking. A straight cherry-pick would have conflicted — `PanicButton.tsx` has been reworked three times since (`97060d1` → `3786f1c` → `4191a83`) — so the `letterSpacing` hunk and the `TRACKING` import were applied by hand instead. `tsc --noEmit` exit 0. **Safe at full size by construction:** `styles.label` spreads `type.label`, whose `letterSpacing` already *is* `TRACKING.label`, so at `scale 1` the inline value is identical and nothing changes; only the two `scale={0.5}` call sites move. The comment was rewritten rather than taken verbatim: `a96ccb4`'s original says the caption scales "for the same reason btnText's does", which is **not true of HEAD**. ✅ **The sibling defect this row deferred is now closed too.** `styles.btnText` set a fixed `letterSpacing: 2` while both call sites render it at `fontSize: 15 * scale`, so the WIPE DEVICE label itself carried exactly the defect just fixed on the caption above it, at both `scale={0.5}` sites. Fixed 29 Aug: `letterSpacing: 2 * scale` added inline alongside the existing `fontSize: 15 * scale` at **both** call sites — the native-glass branch and the `BlurView` fallback (`PanicButton.tsx:341`, `:361`; the `:332`/`:352` line numbers this row originally cited had drifted). ⚠️ **Deliberately NOT `TRACKING.label * scale`, unlike the caption fix one sentence up.** `type.labelStrong`'s tracking *is* `TRACKING.label`, which is **1**, and `btnText` overrides it to **2** on purpose; reaching for the token here would have halved the button label's tracking at scale 1 — a design change wearing a scaling fix's clothes. `2 * scale` keeps scale 1 byte-identical and moves only the two `scale={0.5}` sites, the same by-construction safety the caption fix has. `npx tsc -p tsconfig.json --noEmit` exit 0. Still unverified on device, like the rest of the WIPE DEVICE work — see GF-14. Branch `worktree-panic-glass` held nothing unported and has since been archived and deleted; see the branch-cleanup row below. |
| Stray branch cleanup — three dead branches archived and deleted | ✅ 29 Aug | Ref hygiene after the three worktree ports. No code changed. **Archived first, then deleted.** Three annotated-free tags were cut and pushed to origin *before* any branch was removed, so every tip survives independently of the branches: `archive/panic-red-outline` → `834d01b`, `archive/panic-red-outline-local` → `c63f9f8`, `archive/panic-glass-root` → `3786f1c`. These are the repo's first tags — `git ls-remote --tags origin` was empty beforehand, so `git push origin --tags` pushed exactly these three and nothing stray. **`c63f9f8` is the one that mattered:** it existed *only* as a local ref (its remote branch was one behind at `834d01b`), and it is now on GitHub as `archive/panic-red-outline-local`. Its patch was never at risk either — `git cherry` reports both of that branch's commits as already-upstream, landed as `b9722be` + `4191a83` (see the WIPE DEVICE row). **Deleted local:** `panic-glass-root` (`3786f1c`) and `worktree-onboarding-recovery-guard` (`b16bce0`) with plain `-d`, both being ancestors of `feat/push-notifications`; `worktree-panic-red-outline` (`c63f9f8`) needed `-D`, since its commits reached this branch as cherry-picks rather than a merge and so it read as unmerged. **Deleted remote:** `origin/worktree-panic-red-outline` and `origin/panic-glass-root`. `git worktree prune` run; the two live worktrees (`ghostface-clean`, `.ghostface-devtest`) are both intact. **Preserved deliberately:** `worktree-panic-glass` — see the panic-caption row above; it holds `39ad293`/`a96ccb4` and is the provenance for `db289f4`. Untouched local and remote. **Also this pass:** `devtest` pushed `b94a42d..64cf71d` (`port(eas): bring the cloud-build fixes onto devtest`), clearing its one unpushed commit; its worktree still carries the untracked `devbuild*.log` pile (GF-14). **Re-verified, not acted on:** `39ad293` holds nothing unported — its glass surface is superseded by `4191a83`, its sweep colour `rgba(255,59,48,0.32)` is live at `PanicButton.tsx:404`, its caption tracking landed as `db289f4`, and its `scale={0.8}` was a parallel choice never adopted here (`97060d1` set `scale={0.5}` on this line of work; both call sites still pass 0.5). The one live defect it points at — `styles.btnText` fixed `letterSpacing: 2` against `fontSize: 15 * scale` at `PanicButton.tsx:406` — remains open by choice, as recorded in the panic-caption row. |
| Per-category locks (shared LOCK PIN) | ✅ 26 Aug, build 113 (14) | Extend the wallet-lock pattern to every radial-menu category (MSG/CALL/VPN/WALLET/NUMBER/SETTINGS). One shared **LOCK PIN** (reuses the wallet-PIN storage, still can't match the main PIN) gates whichever categories are toggled in **Settings → CATEGORY LOCKS**; entering it once unlocks all locked categories for the session. AppContext `lockedSections` (persisted, migrates existing wallet-PIN users to a locked wallet), `isSectionLocked`/`setSectionLocked`; reusable `components/SectionLock.tsx` wraps the 5 tab screens, wallet keeps its own gate repointed. Radial nodes show a **gold lock badge** when locked. Commit 4febac0 on `devtest`. tsc clean. |
| Keyboard covers PIN buttons on device | ✅ fixed 26 Aug, build 119 (14 + Air) | The software keypad hid the confirm/unlock buttons on the **category unlock gate** (`SectionLock`, which had NO keyboard handling) and the **LOCK PIN + Recovery Phrase** settings modals — so you couldn't unlock a locked category, set a lock PIN, or retrieve your recovery phrase on a real device. **Fix:** React Native's own `KeyboardAvoidingView` (behavior padding) on all three surfaces; removed `react-native-keyboard-controller` from settings (it's unreliable inside RN `<Modal>` — needs a nested KeyboardProvider, and even then didn't lift on device). Commit 5295dc0. ⚠️ **LESSON: the simulator masks software-keyboard bugs** — it uses the Mac hardware keyboard, so the on-screen keypad never showed and the button looked fine on the sim while being covered on device. Test keyboard/layout on a real device, or force the sim's software keypad (Cmd+K). Cost ~4 wasted sim-verified builds before this was understood. |
| VoIP call alerts | ⚠️ 27 Aug — reliable only on TestFlight build 63 | **Symptom:** backgrounded incoming calls arrived as a **message**, not a CallKit ring (open-app calls work over WebSocket). **Cause:** CallKit-while-closed needs a **production** VoIP push token, which only a **production build** registers (`aps-environment=production`) to match the server (`APNS_ENV=production`); locally **sideloaded builds are sandbox** and never register a working one. **Fix that works:** install a **production build via TestFlight (build 63)** → registers a real production token (`voip` len 64) → calls ring. **Two self-inflicted regressions to AVOID:** (1) 🚫 **never flip `APNS_ENV` on the live server** — a mismatched push makes APNs return BadDeviceToken and the server's `delivery.ts` **auto-deletes the real device token** (this zeroed `GHOSTFACE.voip` 64→0). (2) ⚠️ **sideloading a dev build on top of a seeded token is UNRELIABLE** — it HELD on the 14 (kept voip 64) but the Air's token was CLEARED again (voip 0) on the next call, because a sideloaded (sandbox) build doesn't reliably RECEIVE the production push. **Reliable rule: keep the RECEIVING device on TestFlight build 63; don't sideload the dev build over it.** **Clean long-term fix:** a fresh **production TestFlight build** (post-GF-01) = current features AND reliable VoIP together. **State 27 Aug:** 14 on build 120 holding voip 64 (working); Air on build 120 with voip 0 (NOT ringing) — re-seed the Air via TestFlight 63 and leave it there. Side note: `/push/:id/register` does not bump `updated_at`, so trust token VALUES not timestamps. |
| ⚠️ INCIDENT: wiped littys' unrecoverable identity | ✅ recovered 27 Aug (fresh LITTY) | While chasing the VoIP issue I **uninstalled GHOSTFACE on the Air**, which cleared its keychain (iOS ≥10.3 deletes keychain on app delete) → **LITTY's private key was lost**. littys had **never saved the recovery phrase** (because the keyboard bug had blocked the recovery-phrase modal), so the identity was **unrecoverable**. Resolution: deleted the orphaned LITTY server identity (identity_keys/prekeys/device_tokens) to free the alias, littys re-registered LITTY fresh. **LESSON: never wipe/uninstall a device whose recovery phrase is not confirmed saved.** Fix shipped: **recovery-phrase screens now allow screenshots** (lift the global screen-capture block only on the onboarding recovery step + Settings recovery modal; commit on `devtest`, build 120) so users can actually save the phrase. |
| End-to-end profile photos | ✅ 26 Aug, build 115 — VERIFIED 14↔Air | Your profile pic now reaches contacts **E2E-encrypted** (server sees only ciphertext), staying true to "NO FACE. NO TRACE.". Resized to ~128px JPEG (`expo-image-manipulator ~14.0.8`, **new dep** + pod) and sent as a silent control message (new `p` field in the sealed envelope, validated data:image URI, 400KB cap) — no bubble, like a reaction. `Conversation.contactPhoto` (persisted in the encrypted blob); `broadcastProfilePhoto` fires on set/change and once-per-session on connect (covers offline contacts); receive handling on both established + first-message paths; messages-list + chat-header avatars render the photo with letter fallback (expo-image). Commit b8ba50c on `devtest`. tsc clean. ✅ **E2E delivery verified across two devices (26 Aug)** — a pic set on one device shows as the avatar on the other, end to end. |
| iPhone Air registered as a test device | ✅ 26 Aug | littys' **iPhone Air** (UDID `00008150-000A25AE2212401C`) set up for development: Developer Mode enabled, trusted, and **registered in the Apple Developer account** via the App Store Connect API (the ASC key can register devices; `-allowProvisioningUpdates` alone won't auto-register a brand-new one). Now in the provisioning profile, so future builds install to it directly. On build 115. |
| Lock entry: obsidian glass foil + cosmetics batch | ✅ 26 Aug, build 112 (14) | Entry screen reworked (`app/lock.tsx`, commit 7ec1c0e on `ui-tweaks-25aug`). Full-screen gold seal you **hold to enter**: fingerprint fills base-up, foil peels from centre, and the branded GHOSTFACE gate is now revealed (previously skipped) before the PIN keypad. Tiny “NO FACE. NO TRACE.” embossed top-right. Trigger is press/hold (fires reliably on-device) not a PanResponder rub (which never registered on the physical 14). Biometric: on a real **fingerprint** reader (Touch ID) the hold fires the system scan via LocalAuthentication and unlocks on match; Face-ID-only devices (every current iPhone incl. the 14/16) fall back to the cosmetic hold — PIN remains the real gate there. No new deps, tsc clean. ✅ Merged `ui-tweaks-25aug` → `feat/push-notifications` (pushed to origin); UI work now on `devtest`. **26 Aug cosmetics batch (build 112 on the 14):** (a) **Foil → obsidian smoked glass** — dark frosted GlassView with the hooded gate glowing through, gold now the *accent* (fingerprint / HOLD TO ENTER / corner sign) not the whole surface; verified on the iPhone 17 Pro simulator. (b) **Onboarding polish** — gold-hairline confirm button (was hard white), red→gold "perk" promo, cleaned header, gold-tinted inputs; ✅ **verified on the iPhone 14 (26 Aug)**. (c) **Messages list** — hairline inset dividers, unread rows brighten, and **no message-content preview** (privacy: show only alias + neutral status "Encrypted"/"N new messages", never conversation text). Commits on `devtest`: foil 5b19052/740a5a6/f0c33c7, onboarding 84c6caf, messages da9813b/c030019. ✅ Onboarding verified on the iPhone 14. (Merge needed `pnpm install` for the apple-targets/xcode deps.) |
| Android APK crashes on launch (opens then closes) | ⏸ deferred — needs device log | Preview APK (build `a1be77a4`, commit 2ff8ec6, and the older 22 Aug `912b417` behave the same) **opens then immediately closes on the physical Android device** — a startup crash before/at first render. **iOS is unaffected** (builds 106–111 run fine on the 14). Ruled OUT: `expo-glass-effect` (Android-safe — `isLiquidGlassAvailable()`→false, `GlassView`→plain View); and the CSPRNG fail-closed gate at `index.js:7` (`assertCsprngHealthy()`) — its 4 detection surfaces are all cross-platform, so it would fail on BOTH platforms or neither, and iOS passes. **Do NOT patch `lib/csprng.ts`** to chase this. Leading hypothesis (unconfirmed): iOS builds from the checked-in `ios/` dir but the Android APK is a fresh `expo prebuild` on EAS — an autolinking/native-surface diff invisible from JS, i.e. Android may have never launched (tracker only ever had Android builds "awaiting test", never confirmed opened). **Next:** `adb logcat \| grep -iE 'AndroidRuntime\|ReactNative\|FATAL'` while launching — a top-level throw prints the JS error verbatim. `adb` is now installed on the Mac (brew android-platform-tools); user turned USB debugging on but deferred plugging in the device while sorting the Google Play / Android developer account. If the log shows `InsecureCsprngError`, the fix is to ADD the missing Android native surface to `hasNativeCsprng()` — never remove the throw. Paid EAS builds frozen until a real log exists (account $128 over credits). |
| Wallet BUY button buys the wrong asset | ✅ 21 Aug | Was: `wallet.tsx:139` derived the MoonPay asset from the selected token tab (FZ→`usdc_sol`, CASPER→`sol`), so BUY never bought the token on screen — and no card on-ramp lists custom SPL tokens anyway. Fixed by decoupling purchase currency from `activeToken` entirely: the button is now **FUND**, and the asset (SOL or USDC) is chosen explicitly in a prompt that states FZ/CASPER can't be card-bought. Also closes the placeholder-address bug in the same path — BUY now **requires a linked wallet** (`connectedWalletAddress`) and offers to open the link flow instead of falling back to the `walletAddress` placeholder. tsc clean, 89/89. Not in builds 64/vc26. |
| Wallet address is a hardcoded placeholder | 🔄 partially mitigated | `AppContext.tsx:1419` and `:3279` set `walletAddress: "GhFc3...x9mKr4"` — a literal string with the ellipsis in it, not a generated key. The money-losing path is closed (FUND now requires a linked wallet — see row above), **but the placeholder is still displayed to users** as the in-app wallet's ADDR, and is still offered by the copy-address control and the RECEIVE modal — so a user can copy it and send funds nowhere. Needs either a real generated non-custodial wallet or removal of the in-app wallet UI until one exists. |
| GFC/FANTASMA not deployed on Solana | ⏸ blocked | Live `GET /api/tokens` shows both still `status: pending`, `mintAddress: null`, target `mainnet-beta`. Devnet deploy attempted 21 Aug → `"Deployer not configured. Set SOLANA_DEPLOYER_KEY"`; nothing was written, records intact. Wallet tabs/balances are therefore showing tokens with no mint on any chain. **26 Aug: tokens renamed** — CASPER→GHOSTFACE COIN/GFC (id 1) and FaceZero/FZ→FANTASMA/FTSM (id 2), in code (a21f153, d22f845) AND prod DB (both Railway UPDATEs run); live `GET /api/tokens` returns GFC + FANTASMA. Still `pending`/un-minted. |
| **SECURITY: token deploy endpoint was unauthenticated** | ✅ 21 Aug (`aaa1a8c`) — **not yet deployed** | Confirmed and fixed. `POST /tokens`, `PUT`/`DELETE /tokens/:id`, `POST /tokens/:id/deploy` and the `GET /admin` console were reachable by anyone; deploy calls `createMint`/`mintTo` with the server's deployer keypair. Now gated by `requireTokenAdmin` — shared `TOKEN_ADMIN_SECRET`, constant-time compare, **fails closed** (503 if the secret is unset). Kept separate from the per-user Bearer auth deliberately: minting is not user-scoped. `GET /tokens` + `GET /tokens/:id` remain public (mobile app reads them for `appTokens`). ⚠️ **Set `TOKEN_ADMIN_SECRET` on Railway BEFORE `SOLANA_DEPLOYER_KEY`, and redeploy the API** — the fix is committed locally only, so production is still running the ungated code. Original finding follows: |
| ~~token deploy endpoint unauthenticated (original finding)~~ | ⬜ superseded by the row above | `routes/tokens.ts` has no auth middleware and is mounted bare at `routes/index.ts:20` (`router.use(tokensRouter)`), exposing `POST /api/tokens/:id/deploy` (createMint + mintTo) and the token CRUD + admin HTML page. Currently inert only because `SOLANA_DEPLOYER_KEY` is unset — **setting that key without adding auth first would let anyone reachable at api.ghostface.co.nz mint using the server's keypair, spending real SOL on mainnet.** Verify properly (grep-level finding only), then gate before configuring the key. |
| Settings version string drifted from app.json | ✅ 21 Aug | Settings showed `GHOSTFACE® v1.0.0` while `app.json` was on `1.0.2`. Now reads `appJson.expo.version` via a JSON import (`resolveJsonModule` already on via `expo/tsconfig.base`, so no new dependency) — same file EAS builds against, so it can't drift again. **Not in preview build 64** (cut before the fix), so Settings still reads 1.0.0 on those installs. |
| Preview builds 64 for on-device smoke test | 🔄 built, awaiting test | Run 21 Aug on Benji's go-ahead. **Preview profile deliberately, not production** — production is `distribution: store`, which is not directly installable, so it can't be smoke-tested without `eas submit` (blocked on GF-01). iOS `db0fd24d-2a8a-407e-bed5-5fd3fe884ada`, Android `50b30ad9-6c69-443c-b16f-b18be05e5698`; both internal distribution, install by opening the build URL on-device. Contains the glass restyle, wallet BUY fix and wallet Phase 1. Clears the CSPRNG gate below **only once actually run on physical devices**. Wire-compat with build 63 expected to be fine (both post-date the 19 Aug AD change `10400e8`; `AD_PROTOCOL_VERSION` still `0x01`) but **unverified** — worth testing 63↔64 messaging while both are installed. ⚠️ **29 Aug — for iOS there is now a newer already-paid artifact: use GF-14's `e5111cf1` (28 Aug, commit `f1df8a0`) instead of the 21 Aug preview `db0fd24d`** if the goal is seeing recent JS/visual work; it is 14 commits behind HEAD rather than a week. Note it is `development:device`, so it is a **dev client** (sandbox APNs — will clear a production VoIP token, and wants Metro unless run on its embedded bundle), whereas `db0fd24d` is a `preview` build. Either clears the CSPRNG gate, which only needs a real device boot. **Android has no newer option:** the latest is `a1be77a4` (26 Aug, commit `2ff8ec6`) — the build the launch-crash row is about, so an Android smoke test is blocked behind that `adb logcat`, not behind a build. |
| Landing page: split to own repo, host conflict resolved | ✅ 21 Aug | `artifacts/site/` (untracked here, referenced nowhere, not a pnpm workspace package) moved out to `~/Projects/ghostface-landing` and pushed to `ghostzeronz-coder/ghostface-landing` — same separation as vault2fa, so app pushes can never redeploy the public site. Found **two hosts fighting over ghostface.co.nz**: GitHub Pages (CNAME, built) and Vercel (`ghostface-landing`, team `ghostfacecommunications`). DNS pointed at Vercel, so Pages was dormant-but-armed; **Pages now disabled**, Vercel is the single host. Also found three different versions of the page in circulation — the live one was an 8 KB placeholder; the current scratch-to-reveal design was in the repo but never named `index.html`. Fixed and deployed. ⬜ **Auto-deploy still not wired**: Vercel's GitHub identity is the `pc7zfphhs2-del` account, which lacks access to the `ghostzeronz-coder` repo; a write invite is pending acceptance. CLI deploys work meanwhile. |
| Privacy + support pages | ✅ 21 Aug, needs legal review | Landing footer linked to `/privacy` and `/support`; both 404'd. **App Store review requires a reachable privacy policy URL**, so this blocked submission independently of GF-01. Both now live. Content drawn from what the code actually does (alias-only registration, opaque `delivery_id` routing, public-keys-only storage, **hashed** push tokens, device-local history, non-custodial wallet) rather than boilerplate. ⬜ Items deliberately left as HTML comments rather than guessed: log/IP retention, statutory-rights wording (NZ Privacy Act IPPs 6/7; GDPR arts 15–22 if EU/UK), named processors, minimum age, subscription-cancellation path, abuse-reporting enforcement. Review with GF-01 counsel. |
| GitHub repo sprawl | ✅ 21 Aug | 13 repos → 4. Deleted 9 confirmed-empty repos (0 KB, no commits, verified immediately before deletion): GHOSTFACE, GHOSTFACE.1.0.1, GHOST, ghostfaceu, ghostzeronz-coder-GF, Ghost-face-Landing, Secure-Ghost-Chat1/2/11. Kept `Secure-Ghost-Chat`, `vault2fa`, `ghostface-landing`, and the `ssh_data` fork (left alone deliberately). Vercel still has 3 duplicate landing projects (`-bt9c`, `-gat1`, `site`) — not cleaned up. |
| Railway topology — ⚠️ **do not delete PgBouncer or Postgres HA** | ✅ mapped 22 Aug | Traced end to end: `api-server` → `pgbouncer.railway.internal` (`DATABASE_URL`) → `postgres-ha.railway.internal` (`UPSTREAM_POSTGRESQL_HOST`, HAProxy `clusterRole: edge`) → Patroni leader `Postgres` (=`postgres-1`, owns the data volume) with `Postgres-2`/`-3` as streaming secondaries, coordinated by `etcd-1`…`etcd-5` (all five listed in `PATRONI_ETCD3_HOSTS`). PITR/WAL archived by pgBackRest to bucket `postgres-pitr-…` on `t3.storageapi.dev`. **PgBouncer and Postgres HA are both in the only path from the API to the database — deleting either is an instant, total outage.** PgBouncer's logs read `0 queries/s` when idle; that is 9 users with nobody active, NOT an unused service. |
| Railway replica right-sizing | ✅ 22 Aug | PgBouncer scaled `sfo` 3 → 2. Postgres HA was **already** at `sfo=2` (the top-level `numReplicas: 3` shown by `get-status`/`get-service-config` is a stale legacy field — `multiRegionConfig` is authoritative), so that command confirmed rather than changed it. Deliberately stopped at 2, not 1: both sit in the sole DB path and a single replica makes an app-wide outage depend on one container restart. Verified with 26 polls of a DB-backed endpoint across both redeploys — **0 failures**. Scaling is CLI/dashboard only; the Railway MCP `update-service` tool explicitly does not handle replicas. |
| Postgres "disk growth" — ✅ **investigated, no problem; earlier flag was wrong** | ✅ 22 Aug | The 12 GB / 43 GB / 18 GB figures from Railway's `DISK_USAGE_GB` **do not reflect actual disk usage**. Measured from inside the database three ways: all databases = **34 MB**, `pg_wal` = **240 MB** (23 files), pgBackRest spool = empty. A listing of the volume root (`/var/lib/postgresql/data`) shows only `pgdata`, `certs`, `lost+found` and three lock files — **~275 MB total**, not 18 GB. The metric grows monotonically (0.22 → 18.16 GB over 7 days), which is consistent with *cumulative I/O* rather than point-in-time usage. Health is good: `archived_count` 0 failures, `ready_files=0` (no archive backlog), and both replication slots (`postgres_2`, `postgres_3`) **active with 0 bytes retained** — so no inactive slot pinning WAL, the classic cause. **Nothing to fix. Do not resize or prune anything on this basis.** |
| Postgres `archive_timeout=60s` — cost note only, not a fault | ⬜ low priority | With `archive_timeout=60s` and 16 MB segments, any WAL activity forces a segment switch every minute, each pushed to the PITR bucket. Measured live: **3 segments in 3 minutes (1/min) while active**, but only **44 in the preceding 22 hours** — so the rate is activity-driven, not constant, and the DB is genuinely idle most of the time. (Note the active-period sample was partly self-inflicted: the scaling redeploys and this investigation's own polling generated the activity.) Segments are zstd-compressed, so bucket growth is far below the 16 MB/segment raw figure. At 34 MB of data and 9 users this is not worth changing; if S3 object count ever matters, raising `archive_timeout` to 300s would cut forced switches ~5× at the cost of a longer RPO. Change via Patroni config, not `postgresql.conf`. |
| Railway: infra oversized | ⬜ | 11 services (3-node Patroni + 5-node etcd + pooler + HAProxy) for **9 registered users** pre-launch. Remaining trims are riskier than replica counts: **etcd 5 → 3 must not be done by deleting services** — quorum is a majority of *configured* members, so deleting two leaves 3/5 (still quorum, but **zero** fault tolerance; one more failure demotes Patroni to read-only). Correct order is `etcdctl member remove`, then update `PATRONI_ETCD3_HOSTS` on all three Postgres nodes, then reload. Separately: **Postgres disk was 12 GB current / 43 GB peak over 24h** with a 6.6 GB memory spike — disproportionate for a relay that stores only queued ciphertext for 9 users. Likely WAL/backup retention rather than table data; worth diagnosing before it costs more. |
| Token deploy has no reset/re-deploy path | ⬜ | Deploy sets `status: "deployed"` + `mintAddress` + `network` (`tokens.ts:245-256`), and both deploy (`:167`) and delete (`:145`) then refuse the record. So a devnet deploy permanently blocks a later mainnet deploy through the API and would surface devnet mints to app users via `appTokens`. Add a reset (status→pending, clear mintAddress) or an explicit re-deploy-to-different-network path before deploying anywhere. |
| Self-hosted WireGuard VPN: native client — code written; local build blocked, EAS is not | 🔄 **28 Aug — blocker proven local-only; no decision outstanding** | `PacketTunnelProvider.swift` has real tunnel logic (WireGuardAdapter-driven) and `native/vpn-tunnel/VPNTunnelModule.swift`+`.m` is a real RN native bridge module (NETunnelProviderManager start/stop/status, wired via `lib/vpnTunnelModule.ts`) — both confirmed correctly linked into the Xcode project by direct `.pbxproj` inspection. **Blocked:** adding the bridge module's files to the main app target causes `fmt` (React Native's pinned C++ dep) to compile from source instead of using RN's prebuilt binary, and that from-source compile hits a real, deterministic `fmt` 11.0.2 vs this machine's bleeding-edge Xcode Clang `consteval` bug — confirmed via a controlled isolation test (2/2 reproducible each way), not fixable by a simple build-setting override (traced why). This is the *second* unrelated pre-release-macOS-27 toolchain bug this session (first was Explicit Modules, now fixed). A real fix means patching `fmt`'s header via a Podfile hook or a build-wide macro undefine — deliberately not applied without asking, since it's outside the scope of "fix the VPN" and has its own risk. Full detail + root cause in STATUS.md 24 Aug entry. **Still open beyond the blocker:** EAS cloud build still lacks a Go toolchain, formal Apple Network Extension entitlement request not yet submitted, `lib/wireguard.ts` (on-device keypair gen) and `AppContext.tsx` wiring not started, nothing committed to git yet. Infra/control-plane side (Hetzner box, `api-server` routes) already live — see 23 Aug STATUS entry. **⚠️ 28 Aug — THE `fmt` BLOCKER IS LOCAL-ONLY AND EAS ROUTES AROUND IT. PROVEN, not reasoned.** The bug fires when `native/vpn-tunnel/` enters the **main `GHOSTFACE` target's** build graph. `scripts/link-wireguard-kit.mjs` does exactly that — it appends `../native/vpn-tunnel` to `mainAppTarget.props.fileSystemSynchronizedGroups` (lines 273–297) — the file is **byte-identical at `b63b233` and HEAD** (`git diff` empty), `b63b233` wired it as `eas-build-post-install`, and EAS build `d176c63d-de45-4b2a-9099-cabdf82b23f9` from that commit **FINISHED clean in 5.9 min**. So the exact configuration that fails 2/2 on this Mac succeeds on EAS: `fmt` 11.0.2 vs Clang `consteval` is a pre-release macOS 27 / Xcode 26.6 problem, not a project one. **Benji's call is therefore not blocking anything** — patch `fmt` only if local device builds are wanted for iteration, and note the dev client is not reaching Metro anyway (separate row), so local iteration is already degraded for an unrelated reason. ⚠️ `d176c63d` was `development:device`, **NOT production** — a store build carrying the tunnel target is still unproven. **✅ Entitlement gap CLOSED 28 Aug.** `app.json` `ios.entitlements` declared only app-groups while `targets/network-packet-tunnel/expo-target.config.js:23` declared `com.apple.developer.networking.networkextension` — and since capability sync mirrors *declared* entitlements, the next `eas credentials` run would have re-disabled Network Extensions on `com.ghostface.app` exactly as it did before (see GF-13). `com.apple.developer.networking.networkextension: ["packet-tunnel-provider"]` is now declared on the main target too. **✅ Personal VPN gap ALSO closed 28 Aug — `com.apple.developer.networking.vpn.api: ["allow-vpn"]` added to the main target.** Proven necessary from code, not assumed: `native/vpn-tunnel/VPNTunnelModule.swift` compiles into the **main** `GHOSTFACE` target (`link-wireguard-kit.mjs` appends `../native/vpn-tunnel` to `mainAppTarget.props.fileSystemSynchronizedGroups`) and calls `NETunnelProviderManager.loadAllFromPreferences` (:206) and `manager.saveToPreferences` (:126) — writing a VPN configuration to system preferences requires Personal VPN on the **container** app's App ID. Before this, `vpn.api`/`allow-vpn` appeared **nowhere in the repo's entire git history** (`git log -S`, zero hits), which is why the 25-Aug sync could only turn it off. Without it the failure is at *runtime*, not signing: `saveToPreferences` rejects and the app never reaches `startVPNTunnel()` — invisible until a real on-device connect attempt. ⚠️ **Open question, decide before spending the 2FA cycle:** `targets/network-packet-tunnel/expo-target.config.js:17` claims Personal VPN is "required on this target's App ID", and the 25-Aug sync did disable it there — but that comment is **unsourced**: the reference it cites (`.agents/skills/apple-targets/network-packet-tunnel.md`) does not mention Personal VPN, `vpn.api`, `allow-vpn` or capabilities anywhere, and the claim traces back only to a by-hand 23-Aug portal setup. Conventionally the extension needs Network Extensions + App Groups only, and the container holds Personal VPN — which is exactly the arrangement now declared. **Left as-is deliberately**; the next credentials run will therefore leave Personal VPN OFF on `com.ghostface.app.tunnel`. If an on-device connect later fails inside the provider, declare it on the tunnel target too and re-run credentials (one more 2FA cycle). ⚠️ **This change gates EVERY iOS build until `eas credentials` is re-run — dev-client profiles included, not just production.** It is the same failure mode that burned build 65: a committed entitlement that the provisioning profile does not carry fails signing. Profile `89A3CY9F86` currently does **not** include Network Extensions, because capability sync disabled it (see GF-13). Re-run interactive `eas credentials` (real Apple ID + 2FA, two passes) so both profiles regenerate carrying the capability **before kicking off any build**. **✅ DONE 28 Aug — the run completed and reports "All credentials are ready to build" for both `com.ghostface.app` and `com.ghostface.app.tunnel` (`@ghost_face/mayybachh`), so the signing gate is cleared.** ⚠️ Reported, not verified: that line proves each target has a profile EAS accepts, but the capability-sync output was not captured, so it is still unconfirmed whether Personal VPN was enabled on the main App ID, whether it was disabled on the tunnel App ID as predicted, and whether `89A3CY9F86`/`76QM629KQ5` are still the current profile IDs. Verify from a real build's embedded profile before treating the VPN capability as live on device. ⚠️ **Now the critical path: the formal Apple Network Extension entitlement request is still unsubmitted.** `packet-tunnel-provider` needs Apple to grant it for App Store distribution — weeks of latency, and it is independent of GF-01, of the `fmt` question and of the build path, so it should be submitted now rather than sequenced behind anything. Still not started: `lib/wireguard.ts` (on-device keypair gen — confirmed absent 28 Aug; `@noble/curves` already ships `x25519`, so no new dependency) and `AppContext.tsx`'s `connectVPN()`/`disconnectVPN()`, still mocks. |

| Scaling: `messages`/`departures` pending indexes + purge job | ✅ 24 Aug | **Applied to production.** Both tables were read on every reconnect via `WHERE to_delivery_id/to_alias = $1 AND delivered = false` with **no index at all** (only PKs existed — confirmed against prod `pg_indexes`), so each poll was a seq scan of the whole table; cost grew with lifetime message volume while frequency grew with subscribers, i.e. quadratic. Added partial indexes `idx_messages_pending` / `idx_departures_pending` (`WHERE delivered = false`, matching the `idx_ghost_numbers_next_rotation` precedent — index stays sized to the live backlog, not lifetime volume), created with `CREATE INDEX CONCURRENTLY` direct to the Patroni leader via `DATABASE_PUBLIC_URL`, **deliberately not via `drizzle-kit push`** (push diffs the whole schema against prod and there are no migration files to review — bad blast radius for two indexes). Verified `indisvalid`/`indisready` = t and planner picks the index under `enable_seqscan=off`. Also added `lib/purgeScheduler.ts`: hourly, `pg_try_advisory_lock` key `7424211920n` (rotationScheduler pattern, so it stays correct if replicas ever go >1), deletes delivered rows (unreachable — every read path filters `delivered = false`) and undelivered rows older than **7 days**; batched 5k by `ctid`, 20-batch/tick cap so it can't hold a long lock on the live send path. Prod at time of change: 231 messages / 213 delivered, 13 departures / 8 delivered. |
| ⚠️ `delivered` does **not** mean delivered — client never acks | 🔄 **committed 28 Aug (`11f5c3a`, deployed to prod) — server half LIVE, needs an app release for the client half** | **Fix written and verified, not yet committed** (report-before-code: sitting in the working tree for review). Client (`AppContext.tsx`) now sends `{type:"msgAck", msgId}` after a message is decrypted *and* persisted locally — at every genuine persist point (established-session message/reaction/photo/collision-guard, the X3DH-bootstrap equivalents, and the permanent-decrypt-failure placeholder, since without an id-based dedup there it would otherwise spam a fresh placeholder every reconnect) but deliberately NOT on a transient drop where nothing was actually saved (glare-loss, unknown reaction/photo target pre-session). New in-memory `ackedMsgIdsRef` set (session-lifetime only) makes a redelivered message re-ack instead of re-decrypt — Double Ratchet consumes a message key per decrypt, so blindly retrying a redelivered ciphertext is not safe, and this is the `msgId` dedupe this row originally called for. Server: the three optimistic `delivered = true` writes are gone — `deliverPending` (`ws/manager.ts`) and the live `msg` send path just log "sent, awaiting ack" now; `GET /messages/pending` (`messages.ts`) no longer marks anything, it only peeks (see below). The dead `type: "ack"` handler is renamed `msgAck` and now **deletes** the row instead of flagging it (`markMessagesDelivered` removed — replaced by `deleteAckedMessages` in `utils/markDelivered.ts`), closing the original "rename one, same word means two things" note. ⚠️ **`GET /messages/pending` still has no ack of its own** — nothing in the current app calls it (looks like a legacy pre-WS fallback still on the server for old installs); it now returns the backlog without consuming it, so a caller on that path re-fetches the same rows until the existing 7-day purge job clears them, same posture as any pre-ack legacy client. **Verified:** api-server typecheck clean, 78/78 tests pass, eslint clean; app typecheck clean, 112/112 tests pass, eslint shows the same 9 pre-existing warnings as before the change (confirmed via `git stash` diff, nothing new). **Still gated on an EAS build/submit** (needs its own explicit go-ahead, and App Store submission specifically stays behind GF-01 regardless) before this reaches any real device. |
| Scaling: N+1 UPDATE on every pending fetch + implicit pool size | ✅ 24 Aug | `routes/messages.ts:100` and `ws/manager.ts:352` issue **one UPDATE round-trip per message** inside `Promise.all` — a user returning to 200 queued messages fires 200 concurrent UPDATEs. Should be a single `UPDATE ... WHERE id = ANY($1)`. Separately `lib/db/src/index.ts` creates `new Pool()` with **no `max`**, so it inherits pg's default of 10. Not urgent: `DATABASE_URL` points at `pgbouncer.railway.internal` (see Railway topology row), so PgBouncer multiplexes behind it and 10 is a client-side cap rather than the real ceiling — but it should be explicit rather than inherited. **Done:** all three N+1 sites (`routes/messages.ts` pending poll, `deliverPending`, `deliverPendingDepartures`) now call the new `utils/markDelivered.ts` helpers, which issue a single `UPDATE ... WHERE id IN (...)` per batch instead of one round-trip per row; chunked at 1,000 ids because Postgres caps a statement at 65535 bound parameters and `inArray` binds one per id (verified generated SQL: `update "messages" set "delivered" = $1 where "messages"."id" in ($2, $3, $4)`). `Pool` now takes an explicit `max` (default **30**, override via `DB_POOL_MAX`, invalid values warn and fall back rather than blocking boot). Sizing evidence: PgBouncer is `POOL_MODE=transaction`, `DEFAULT_POOL_SIZE=300`, `MAX_CLIENT_CONN=1000` across 2 sfo replicas, upstream Postgres `max_connections=500` with 11 backends in use — so 30 is a client-side cap well inside the pooler's budget. ⚠️ If the pooler is ever bypassed, `max × replica count` must stay under `max_connections`. |
| Scaling: rate limiters keyed on IP, not user | ✅ 24 Aug | **Scope correction:** this is not two limiters in `messages.ts` — `getIpKey` has **14 call sites across 8 route files** (`messages`, `invites`, `blobs`, `numbers`, `crypto`, `iceConfig`, `vpn`, `integrity`), every one of them IP-keyed. Mobile carrier NAT collapses thousands of subscribers onto one key, so at scale real users get 429s for no visible reason — **and we ship a VPN**, so our own VPN users all egress from one address and would trip our own limiter collectively. Appears *only* with subscriber growth, which is why it matters now. Worst-affected are the scarce/expensive ones: `numbers.ts` `provisionLimiter` (3/hour — real Vonage spend) and `crypto.ts` `intentLimiter` (10/10min — payment intents); under carrier NAT those budgets are shared by every subscriber on that carrier. Fix is to key per-alias post-auth, but note the trap: today the limiter runs **before** `getAuthedAlias`, so moving it after auth would leave the unauthenticated auth lookup (a DB query) with no limit at all — needs both a coarse IP gate protecting the lookup and a per-alias quota behind it. `/users/exists/:alias` is unauthenticated by nature (it *is* the enumeration surface) and has no alias to key on, so it must stay IP-keyed and needs a different mitigation. ⚠️ **CORRECTION to an earlier claim in this row:** it previously said `getIpKey`'s `x-forwarded-for.split(",")[0]` made the limiter bypassable by header spoofing. **That is not true as deployed** — verified empirically against prod on 24 Aug: burned the 60/min budget on `/api/users/exists/` from one IP until it returned 429, then reissued with `X-Forwarded-For: 203.0.113.77` and still got **429**. Railway's edge overwrites the header, so a client-supplied value never reaches the app. It remains a latent hazard rather than a live hole — it would become exploitable if the service were ever exposed directly or fronted by a proxy that appends rather than replaces. Defence-in-depth fix is `app.set("trust proxy", ...)` in `app.ts` (currently **not set at all**, which is why `getIpKey` parses the header by hand and why the `req.ip` fallback is useless) plus reading the trusted hop rather than `[0]`. Also stale: the comment says "behind the Replit proxy" — we're on Railway. **Done 24 Aug.** Design note: a coarse per-IP gate is useless here — 1,000 NATed users legitimately generate 1,000× one user's traffic, so any ceiling low enough to constrain an attacker also breaks real users. Instead the IP bucket now counts **only failed authentication**: legitimate NATed users never charge it, while an unauthenticated flood is cut off *before* the device-token lookup, so it still protects the DB. Real quotas moved to the authenticated alias. `RateLimiter` gained `allowed()`/`record()` so a caller can gate on prior failures without consuming budget (`check()` kept as allowed+record). Applied to the 4 authenticated files — `messages` (pendingPoll), `numbers` (smsInbox, provision), `crypto` (intent, status), `vpn` (register, gate inside `requireDeviceAuth`) — 6 quotas now keyed per user. The 4 unauthenticated files (`invites`, `blobs`, `iceConfig`, `integrity`) have no alias to key on, so per-IP stays with ceilings raised 10× for carrier NAT, each paired with a new `GlobalLimiter` — a single shared bucket that caps the *resource* (disk, Twilio spend) when the caller cannot be identified. That bounds blast radius; it does not identify an abuser, and the honest fix is auth (see the three rows below). Also set `app.set("trust proxy", 1)` in `app.ts` (was unset) and rewrote `getIpKey` to prefer `req.ip` and, in fallback, take the RIGHTMOST forwarded entry rather than the leftmost client-supplied one. ⚠️ **Couples to the backplane item:** every limiter is still per-process in-memory, so the moment replicas go above 1 each limit silently multiplies by replica count — these need shared state at the same time as the WS maps. |
| ⚠️ `POST /blobs` is an unauthenticated disk write | 🔄 code complete both halves; **enforcement flag OFF — do not flip before the app release** | No auth of any kind; the IP rate limiter was the only gate. Anyone can write arbitrary binary to the server's disk up to `MAX_BLOB_BYTES` per request. Now bounded by a global cap (12,000 uploads/hour across all callers) so it cannot fill the volume, but that caps damage rather than preventing it — and the same cap is what real users share. May well be deliberate (blobs are E2E-encrypted, ids are random UUIDs), but "the rate limiter is the only auth" is a different posture at mass production than at 9 users. Decide: require device auth, or record that open write is intended. **27 Aug — step 1 of 2 done (client).** The app now sends `Authorization: Bearer <device-token>` (plus the alias, because `device_tokens.token_hash` is not unique — only `user_id` is) on this endpoint. New `lib/deviceAuth.ts` holds the shared token/alias reader; `secureGet`/`secureSet`/`secureDelete` were extracted out of `AppContext.tsx`, which is where they were trapped. tsc clean, app suite 112/112. **This is a no-op against today's server** — it accepts and ignores the header. ⚠️ **Step 2 (server enforcement) must NOT deploy before an app release carrying step 1 is out**: no shipped build sends this header, so enforcing first breaks builds 63 and 99. Ride the build-72 AD-cutover window, which is already a coordinated hard update. **27 Aug — step 2 written (server), NOT ENFORCED.** `src/lib/auth.ts` (`checkAuth`) now gates this endpoint, behind `ENFORCE_ENDPOINT_AUTH`, which defaults OFF and ships OFF — behaviour in production is unchanged. While off, every unauthenticated call is logged ("ENFORCE_ENDPOINT_AUTH is off"), which is the signal for when it is safe to flip: once those stop, all live clients send the header. Filter Railway logs by that message text, NOT by severity — verified in prod 27 Aug, Railway surfaces pino `warn` as `info`. Flipping is a Railway variable change, not a deploy, so it is instantly reversible. Tests: 70 passed (was 53) — `auth.test.ts` (9, the shared gate), `iceConfig.test.ts` (5, new), `blobs.test.ts` +3. All hermetic; `lib/auth.ts` imports `@workspace/db` lazily so gating a route no longer drags a DATABASE_URL requirement into its tests.| **Server half DEPLOYED 27 Aug** (`df0d4bc`, deploy `f1c7ee5b` SUCCESS) with the flag OFF — verified live immediately after: unauthenticated `GET /api/ice-config` still returns 200 with real TURN credentials, and the gate logged the expected line. Production behaviour is unchanged; the only remaining step is flipping `ENFORCE_ENDPOINT_AUTH` once an app release carrying the client half (`19db682`) is out.
| ⚠️ `GET /ice-config` hands out Twilio TURN credentials unauthenticated | 🔄 code complete both halves; **enforcement flag OFF — do not flip before the app release** | No auth; returns live Twilio TURN credentials, which is our spend. Common pattern for TURN, but at mass scale an unauthenticated credential vending machine is a direct billing surface. Now bounded by a global cap (30,000/hour across all callers) — that bounds the bill, it does not stop the abuse. Decide: device auth, or accept and set a Twilio spend alarm. **27 Aug — step 1 of 2 done (client).** The app now sends `Authorization: Bearer <device-token>` (plus the alias, because `device_tokens.token_hash` is not unique — only `user_id` is) on this endpoint. New `lib/deviceAuth.ts` holds the shared token/alias reader; `secureGet`/`secureSet`/`secureDelete` were extracted out of `AppContext.tsx`, which is where they were trapped. tsc clean, app suite 112/112. **This is a no-op against today's server** — it accepts and ignores the header. ⚠️ **Step 2 (server enforcement) must NOT deploy before an app release carrying step 1 is out**: no shipped build sends this header, so enforcing first breaks builds 63 and 99. Ride the build-72 AD-cutover window, which is already a coordinated hard update. **27 Aug — step 2 written (server), NOT ENFORCED.** `src/lib/auth.ts` (`checkAuth`) now gates this endpoint, behind `ENFORCE_ENDPOINT_AUTH`, which defaults OFF and ships OFF — behaviour in production is unchanged. While off, every unauthenticated call is logged ("ENFORCE_ENDPOINT_AUTH is off"), which is the signal for when it is safe to flip: once those stop, all live clients send the header. Filter Railway logs by that message text, NOT by severity — verified in prod 27 Aug, Railway surfaces pino `warn` as `info`. Flipping is a Railway variable change, not a deploy, so it is instantly reversible. Tests: 70 passed (was 53) — `auth.test.ts` (9, the shared gate), `iceConfig.test.ts` (5, new), `blobs.test.ts` +3. All hermetic; `lib/auth.ts` imports `@workspace/db` lazily so gating a route no longer drags a DATABASE_URL requirement into its tests.| **Server half DEPLOYED 27 Aug** (`df0d4bc`, deploy `f1c7ee5b` SUCCESS) with the flag OFF — verified live immediately after: unauthenticated `GET /api/ice-config` still returns 200 with real TURN credentials, and the gate logged the expected line. Production behaviour is unchanged; the only remaining step is flipping `ENFORCE_ENDPOINT_AUTH` once an app release carrying the client half (`19db682`) is out.
| ⚠️ `POST /invites` accepts an attacker-chosen `ownerAlias` | 🔄 code complete both halves; **enforcement flag OFF — do not flip before the app release** | Unauthenticated, and the body carries `ownerAlias` — so anyone can mint invite codes attributed to any user, with no check that the caller owns that alias. Global cap (2,000/10min) added; the attribution problem is unaffected by rate limiting and needs auth plus a check that the authenticated alias matches `ownerAlias`. **27 Aug — step 1 of 2 done (client).** The app now sends `Authorization: Bearer <device-token>` (plus the alias, because `device_tokens.token_hash` is not unique — only `user_id` is) on this endpoint. New `lib/deviceAuth.ts` holds the shared token/alias reader; `secureGet`/`secureSet`/`secureDelete` were extracted out of `AppContext.tsx`, which is where they were trapped. tsc clean, app suite 112/112. **This is a no-op against today's server** — it accepts and ignores the header. ⚠️ **Step 2 (server enforcement) must NOT deploy before an app release carrying step 1 is out**: no shipped build sends this header, so enforcing first breaks builds 63 and 99. Ride the build-72 AD-cutover window, which is already a coordinated hard update. **27 Aug — step 2 written (server), NOT ENFORCED.** `src/lib/auth.ts` (`checkAuth`) now gates this endpoint, behind `ENFORCE_ENDPOINT_AUTH`, which defaults OFF and ships OFF — behaviour in production is unchanged. While off, every unauthenticated call is logged ("ENFORCE_ENDPOINT_AUTH is off"), which is the signal for when it is safe to flip: once those stop, all live clients send the header. Filter Railway logs by that message text, NOT by severity — verified in prod 27 Aug, Railway surfaces pino `warn` as `info`. Flipping is a Railway variable change, not a deploy, so it is instantly reversible. Tests: 70 passed (was 53) — `auth.test.ts` (9, the shared gate), `iceConfig.test.ts` (5, new), `blobs.test.ts` +3. All hermetic; `lib/auth.ts` imports `@workspace/db` lazily so gating a route no longer drags a DATABASE_URL requirement into its tests.| `ownerAlias` is now taken from the authenticated token and the body field ignored; while the flag is off an unauthenticated caller still falls back to the body, which is the pre-auth behaviour. The two agree for a real client — the app sends its own alias. **Server half DEPLOYED 27 Aug** (`df0d4bc`, deploy `f1c7ee5b` SUCCESS) with the flag OFF — verified live immediately after: unauthenticated `GET /api/ice-config` still returns 200 with real TURN credentials, and the gate logged the expected line. Production behaviour is unchanged; the only remaining step is flipping `ENFORCE_ENDPOINT_AUTH` once an app release carrying the client half (`19db682`) is out.
| Scaling: WS state is in-process — hard ceiling at 1 replica | ✅ **superseded 24 Aug by the backplane work — re-verified live 28 Aug** | `api-server` runs `multiRegionConfig: {sfo: {numReplicas: 1}}`, and it cannot be raised: `connectedClients`, `deliveryIdToAlias`, `activeCallsByPair`, `pendingCallHangups`, `ghostpadCodes`, `ghostpadPartners` are all module-scope `Map`s in `ws/manager.ts`. With 2 replicas a message for a user on replica B arriving at replica A finds nothing in `connectedClients` and silently falls back to store-and-push; call signalling breaks outright (both legs must land on the same process); ghostpad codes only exist in the process that minted them. **Correctness wall, not a performance slope.** Needs a shared backplane (Redis pub/sub or equivalent) + sticky sessions. Also: one replica means every deploy and crash drops 100% of live sockets at once, and all of them then run `deliverPending` simultaneously. `ws/manager.ts` is already 986 lines and every new message type adds another in-process map — this gets harder to unpick every week. **⚠️ EVERYTHING ABOVE IS HISTORICAL** — kept for the reasoning, not as current state. **✅ SUPERSEDED: the decision was made and shipped 24 Aug in the three backplane commits below; this row was simply never moved. Re-verified against live infrastructure and code 28 Aug:** Railway `api-server` production runs `multiRegionConfig: {us-west2: {numReplicas: 2}}` — **two** replicas, and not in `sfo`, so the config quoted above is wrong on both counts. `REDIS_URL` is present on the service and Redis is provisioned in the project. Of the six maps named above, four are gone from `ws/manager.ts`: `ghostpadCodes`, `ghostpadPartners`, `activeCallsByPair` and `pendingCallHangups` all live in `ws/sharedState.ts` now (the last two survive only as comments at `manager.ts:625` and `:660`). `connectedClients` and `deliveryIdToAlias` are still module-scope **deliberately** — a socket handle cannot be shared (step 3 reaches it by pub/sub) and the latter caches an immutable DB row, so per-replica copies are fine. `ws/router.ts` exists and `manager.ts` carries 16 `router`/`sendToAlias` references. **No architecture decision is outstanding.** |
| Scaling: push send is inline in the WS message handler | ⬜ | `ws/manager.ts:859-861` — on a message to an offline user, the handler `await`s a DB read (`pushTokensForDeliveryId`) **and** an HTTPS round-trip to Expo before the send completes, on the single event loop. At volume this is the throughput limiter on the socket path. Should be queued, not awaited. Falls out naturally of the backplane work above. |
| Scaling: WS backplane — step 1 of 3 (Redis + shared rate limits) | ✅ 24 Aug | First of three deployable commits toward lifting `numReplicas: 1`. Redis service provisioned on Railway (`railway add --database redis`), `REDIS_URL` wired to `api-server` as a reference variable, `ioredis@6` added (approved — pure JS, no native bindings; confirmed esbuild inlines it, bundle 4.8→5.2 MB). New `lib/redis.ts` holds the command client plus a separate subscriber connection (Redis puts a socket into subscriber mode exclusively, so pub/sub cannot share the command socket); `enableOfflineQueue: false` and `maxRetriesPerRequest: 1` so a dead Redis fails the command instead of hanging a request behind it. `RateLimiter` is now async and Redis-backed via `INCR` + `PEXPIRE` in one round trip — atomic, so two replicas cannot both read an under-limit count and both admit the request the way a GET-then-SET would. **Discharges the coupling flagged on the rate-limiter row:** limits are now shared across replicas rather than multiplying by replica count. `REDIS_URL` unset is a supported mode (dev, tests) and a Redis outage mid-flight falls back to the per-process bucket rather than failing open — that matters for `provisionLimiter`, which spends real Vonage money, so an outage makes limits per-replica but never absent. All 14 call sites awaited; every limiter got an explicit stable `prefix` for its Redis namespace, and the four `authFailureGate`s deliberately share one namespace so an attacker failing auth across endpoints charges a single budget. Still at 1 replica — nothing observable changed. **Next: step 2** (move ghostpad codes, call state, pending hangups and presence keys to Redis, still at 1 replica) then **step 3** (pub/sub routing + duplicate-connection kick, then raise replicas). ⚠️ Step 2 must use `SET <code> NX EX` for ghostpad codes — `generateGhostpadCode()`'s `Map.has()` uniqueness loop stops being a uniqueness check once codes are shared, and two replicas minting the same 6-digit code would pair a joiner with the wrong person. Presence TTL must be ~3× the 30s heartbeat (90s) or a late refresh flickers live users offline. |
| Scaling: WS backplane — step 2 of 3 (shared state) | ✅ 24 Aug | Every module-scope Map in `ws/manager.ts` that pinned the service to one replica now lives in `ws/sharedState.ts`, Redis-backed with the original in-process Map as fallback when `REDIS_URL` is unset or Redis is mid-outage. Migrated: ghostpad codes + pairing, active calls, deferred hangups, presence, and presence subscriptions. **Still at 1 replica — no observable change**, which is the point: this is verifiable before routing moves. Deliberately NOT migrated: `connectedClients` (a socket handle cannot be shared — step 3 routes to it via pub/sub) and `deliveryIdToAlias` (a cache of an immutable DB row; per-replica copies are fine). **Correctness details:** ghostpad codes are claimed with `SET NX PX` and redeemed with `GETDEL`, so two replicas cannot mint the same code and two joiners cannot both redeem one — the old `Map.has()` loop would have paired a joiner with the wrong person once codes were shared, a cross-user leak rather than a glitch. `clearActiveCall` uses a Lua compare-and-delete so a concurrent `call-ring` that replaced the entry isn't clobbered. Added reverse indexes (`watchers:<target>`, `call:alias:<alias>`) because the old code scanned whole Maps in `notifyPresence` and `cleanup` — against Redis that would have been a keyspace scan. `sweepExpiredGhostpadCodes` deleted; key TTLs replace it. Presence TTL is 90s against the existing 30s heartbeat (3×, so two missed refreshes are tolerated — at 1× a late tick flickers live users offline); the heartbeat now refreshes presence for every alias the replica holds. ⚠️ **Metadata note:** presence subscriptions are the closest thing this system has to a social graph, and they were in-memory-and-die-with-the-socket by design. They now live in Redis, keyed per alias, TTL'd, and deleted on disconnect — so the graph exists only for the span of an open connection and never reaches Postgres, which stays free of any relationship table. A real softening of the posture, accepted 24 Aug, recorded as such. **Test fix:** `flush()` in callSignals/departures tests counted exactly 5 microtask turns, which silently broke when the handlers grew awaits; it now yields the macrotask queue once so it drains whatever is pending. Not a behaviour change — the same frames are sent, one tick later. **Next: step 3** — pub/sub routing to `connectedClients`, duplicate-connection kick (one device per user, but two replicas can each hold a socket for the same alias with neither aware), then raise `numReplicas`. |
| Scaling: WS backplane — step 3 of 3 (pub/sub routing) | ✅ 24 Aug — **running at 2 replicas** | New `ws/router.ts`: each replica subscribes to `ws:u:<alias>` for every socket it holds, and any replica reaches an alias by publishing there — so **no routing table and no sticky sessions**; any replica can accept any socket. All 11 cross-socket send sites in `manager.ts` now go through `router.sendToAlias`; `connectedClients` is left for purely local work (kick handler, heartbeat refresh, cleanup, registration). **Delivery semantics improved, not just relocated:** the call relay used to branch on `recipient.ws.readyState === OPEN`, which the code's own comment notes lies for tens of seconds after a backgrounded socket dies — and which cannot see a socket on another replica at all. It now branches on whether the frame was actually written. `waitForReconnect` polls shared presence instead of the local map, since a VoIP-woken device may reconnect to a different replica. ⚠️ **Self-publish trap handled:** a replica stays subscribed to its own channel while it holds an alias, so PUBLISH counts it as a receiver. If the local write already failed, that receiver is us failing again — `sendToAlias` subtracts self before deciding, or it would report success for a dead socket. **Duplicate-connection kick:** `deviceTokensTable.userId` is unique (one device per user) and within a process the `Map.set` superseded an older socket; across replicas two can each hold a live socket for the same alias with neither aware. Each connection now carries a `connId`; on auth the replica publishes to `ws:kick` and any other holder closes its socket with 4002. **Robustness fix found via a test:** the local delivery promise waited on `ws.send`'s callback unbounded. `ws` always fires it, but if it ever didn't, the message handler for that socket would wedge and every frame routed to that alias would queue behind it — now bounded at 5s, timing out as *undelivered* so the caller falls back to a wake push (a spurious wake is far cheaper than a dropped message). The test fakes were also dropping the callback, which is what surfaced it; they now invoke it as the real `ws` does. **Remaining: raise `numReplicas` above 1.** Deliberately NOT done in the same change — the code is deployed and verifiable at one replica first, and the replica count is a one-line, instantly reversible config change. **Done — `numReplicas` is now 2 and verified in production.** Both replicas boot with `[redis] ready` (commands + subscriber) and `[router] subscriptions armed`; Railway balances traffic across both (confirmed by two distinct container hostnames in the request logs). ⚠️ **A bug the staged rollout caught, which would have been invisible at 1 replica:** `onKick` ran during `createWsServer`, before the Redis socket was up, and with `enableOfflineQueue: false` that SUBSCRIBE failed immediately while `wired` was already set — so the kick channel was never subscribed and never retried. The duplicate-connection kick would have been silently dead the moment replicas went above one, presenting as "two devices both ring" with nothing in the logs after the first line. Fixed by letting the subscriber connection queue offline commands (it serves no request path) and by re-arming every subscription on each `ready` — ioredis drops all subscriptions on reconnect, so a Redis blip would otherwise strand a replica's sockets unreachable, silently. ⚠️ **Region moved `sfo` → `us-west2`.** `railway scale` rejects the legacy `sfo` name when *setting* a count, so it created a second region entry (briefly 3 replicas across two entries) before `sfo=0` was accepted to remove it. Postgres, PgBouncer and Redis all remain in `sfo`. Measured after the move: DB-backed `/api/users/exists` server-side `responseTime` is **3–9 ms vs a 5–6 ms baseline in `sfo`** — no cross-region penalty, so `us-west2` is effectively the modern id for the same location. **Note for future scaling: the CLI cannot recreate an `sfo` entry**, so going back would need the dashboard. **LIVE TWO-DEVICE VERIFICATION (24 Aug).** With `AYYBACHHFKUOUMAY` on `ed3a8b6597d1` and `PHANTOM_9` on `6359e92f7dfa` — genuinely different containers — call signalling relayed **in both directions across the replica boundary**: `call-accept` A→B logged by `ed3a8b6597d1`, `call-ring` and `call-offer` B→A logged by `6359e92f7dfa`. `Call signal relayed` only logs when `sendToAlias` returned true, i.e. the frame was actually written to a socket, so this is delivery and not merely a publish. Call signalling is the case that failed **outright** pre-change (both legs had to be in one process). The duplicate-connection kick also fired three times, each from a different replica than the one closing the socket. ⚠️ **Not proven: cross-replica *message* delivery.** `Message delivered live msgId=232` was logged, but device A had reconnected onto `6359e92f7dfa` at 12:20:29, so both clients were co-located by then — that was a local delivery. Same for the later `call-hangup` and `call-ice` frames. The message path uses the identical `sendToAlias` call the signalling already exercised, so there is no real doubt, but it has not been observed cross-replica. To close it: force a reconnect until the two devices split across replicas again, then send a message and look for `Message delivered live` on the sender's replica. Both schedulers (rotation, purge) already guard with `pg_try_advisory_lock`, so only one replica does that work. Cost is now 2x compute for this service. |
| Twilio TURN returns 401 — account suspended; coturn is carrying TURN | ⬜ low priority, **not a call blocker** | Found 24 Aug live-testing calls. `GET /api/ice-config` logs `Twilio NTS unavailable, falling back` with `Twilio NTS request failed: 401 {"code":20003,"message":"Authenticate"}` at `routes/iceConfig.ts:126`. **Cause: the Twilio account is suspended** (confirmed by Benji), not a rotated key. ⚠️ **An earlier version of this row said calls would have no audio behind symmetric NAT. That was wrong** — checked by querying the endpoint directly (it is unauthenticated, so no auth needed): it returns `source: "coturn"` with our own TURN at `turn:204.168.139.146:3478` and HMAC credentials. The fallback chain (twilio → coturn → static → stun-only) worked exactly as designed and TURN is being served. TCP 3478 on that host confirmed open. Caveat: the coturn credentials are computed locally from `TURN_SECRET`, so serving them proves configuration rather than that coturn is relaying — a real two-device call on **cellular** (not wifi) is what would confirm media actually flows. ⚠️ **Correction to an earlier version of this row: `204.168.139.146` is NOT the VPN box.** Verified 24 Aug by SSH — it is `ubuntu-4gb-hel1-1` (Hetzner Helsinki), runs coturn only, has no `wg0` interface (just `eth0`/`lo`) and no `vpn-agent` unit. The VPN box is **`88.99.225.231`**, per `VPN_AGENT_URL` and `VPN_SERVER_ENDPOINT` on the Railway api-server. TURN and the VPN control plane are on **separate machines**, so firewalling the VPN agent's 8443 carries no risk to TURN. Twilio is referenced in exactly one file (`routes/iceConfig.ts`) and nothing else depends on it, so the suspension has no other blast radius — decide whether to restore the account, drop the Twilio path entirely, or leave it as a dormant first choice. |
| Client reconnect churn — one alias re-authenticated 6x in 4 min | ⬜ **needs a look, may be benign** | Observed 24 Aug during the two-device test: `AYYBACHHFKUOUMAY` authenticated at 12:17:24, 12:19:14, 12:19:34, 12:20:29, 12:21:46 — hopping between replicas each time, each reconnect firing a duplicate-connection kick and re-running `deliverPending`. May simply be the tester foregrounding/backgrounding two handsets, which drops the socket by design. But if the client is reconnect-looping on its own it is worth finding: at scale every cycle costs a pending-messages query plus a kick publish, and it is the kind of thing that looks like server load. Build 63's link-quality indicator (`linkStatsRef` / `recomputeLinkQuality` in `AppContext.tsx`) should show whether the client thinks the link is unhealthy. Contrast: `PHANTOM_9` authenticated once and stayed connected throughout. | ⓘ 27 Aug: still occurring, and every churning alias in the 28 Aug 00:15 UTC window was **GHOST**, the sideloaded build — while GHOSTFACE, on TestFlight build 63, was in the same window and does not appear in the churn. Suggestive, not conclusive (one window, and the two devices differ in more than signing). Worth confirming against an ASC-signed install before investigating the backplane.
| Recovery phrase at sign-up — ✅ **already built, my earlier row was wrong** | ✅ verified 24 Aug | ⚠️ **CORRECTION.** An earlier version of this row claimed the reclaim endpoints were unusable because the client never surfaces the identity key. **That is false, and I asserted it twice without checking the client.** The whole feature already exists end to end: `lib/recoveryPhrase.ts` encodes the IK private key as a 24-word BIP39 phrase (entropy-as-key, so it backs up the *exact* existing key rather than deriving a new one); `AppContext` exposes `getRecoveryPhrase()` and `recoverIdentity()`; `app/onboarding.tsx` has a dedicated `recovery` step in the `alias → pin → recovery` flow that shows the 24 words, warns "WRITE THIS DOWN — IT'S THE ONLY WAY TO RECOVER YOUR IDENTITY IF YOU LOSE THIS DEVICE", and gates continuing behind an "I've saved it" checkbox; there is a `restore` step for entering a phrase; and `app/(tabs)/settings.tsx` has a PIN-gated viewer. Server side (`/prekeys/:userId/reclaim/challenge` + `/verify`, X25519 DH + HMAC-SHA256 proof) was already confirmed live. **So reinstalling does NOT have to burn an alias** — a user who saved their phrase can reclaim it. ✅ **Copy contradiction fixed 24 Aug.** Onboarding claimed "WE NEVER STORE IT AND CANNOT SHOW IT TO YOU AGAIN" while Settings *can* show it again behind the PIN — the app was telling users something untrue about itself. Kept the Settings viewer (PIN-gated, and it is the safety net for anyone who loses their written copy while still holding the device) and corrected the wording instead: it now says the phrase is never stored server-side, that losing both the phrase and the device loses the alias, and that it can be viewed again in Settings while the device is in hand. Urgency preserved, claim now accurate. 🔎 **Answered 24 Aug, and it was not a recovery problem at all.** The handset that appeared to reset to sign-up recovered on its own, and the logs show how: `LITTY` re-authenticated with its **existing device token** (`GET /prekeys/LITTY/count` then `WS client authenticated`, 23:42–23:43) — **no `reclaim/challenge`, no `reclaim/verify`, no re-registration**. Its credentials were in secure storage the entire time. So the app rendered the onboarding screen while holding a valid device token and identity keys, which to a user is indistinguishable from losing everything. **That is the actual bug: a spurious sign-up screen, not credential loss.** Worth finding, because it will read as catastrophic data loss to anyone it happens to. ⚠️ **Consequence: the reclaim path is still completely unexercised in production.** Nothing has ever run it for real, so "recovery works" remains an assumption. Worth a deliberate test: note a fresh alias's phrase, uninstall, reinstall, restore. 🔎 **Minor, found while checking:** `reclaim/verify` updates `spkPublicKey`/`ikSign*`/`pqkem*` but never sets `updatedAt`, so a reclaimed identity still reads as never-updated. Misleading for exactly the forensics above — I initially misread an unchanged timestamp as proof no reclaim had run. ⚠️ **Operational note that still stands:** deleting an identity server-side logs out any installed device for that alias. Recoverable via the phrase, but not silently — it is not a test-data-only action. |
| WireGuard server key rotated | ✅ 24 Aug | Rotated because the previous private key was exposed: an agent ran `cat /etc/wireguard/wg0.conf` while verifying the config-corruption fix, and that file contains `PrivateKey`. It landed in a session transcript — not public, but a production secret somewhere it should not be. **Lesson: filter secrets out of config dumps** (`grep -v PrivateKey`), the same way the vpn-agent deliberately never logs headers. Old pub `UMtYeMUINFIuXTl5hEYzEJEKHae3gFsv42pHPmdmlyU=` → new pub `vnSl8U9ay4QW3K/Jpk6AZTA62R1pUUgs48zzMoo+pmA=`. Rotated on the box (config backed up to `wg0.conf.bak-<date>`, applied live with `wg set wg0 private-key`, temp key file `shred`ed), then `VPN_SERVER_PUBLIC_KEY` updated on the Railway api-server and redeployed. Verified by a throwaway register round-trip: the API now returns the new `serverPublicKey`; peer and identity deleted afterwards. ⚠️ **This was near-free only because there were 0 peers.** Every client caches `serverPublicKey` from its registration response, so post-launch a rotation breaks every existing tunnel until each device re-registers — and until the recovery-key work lands, some users would not be able to re-register at all. Treat key rotation as a migration once there are real users. |
| Stale `~/Downloads/Secure-Ghost-Chat` deleted; one unported commit preserved | ✅ 25 Aug | The 2.2GB Replit-era duplicate repo in Downloads was checked before deletion. Its `fix/call-dedup` branch was 7 commits ahead of its (Replit) upstream, all dated 17 Aug: six were already ported or superseded in this repo (Cipher/Stealth → `lib/stealthCrypto.ts`; gold-glass styling → the 21 Aug restyle; LIVE badge already absent; coin isolation evolved separately), but **`190bafa` — inbound voice webhook scaffolding for ghost numbers (`/webhooks/voice/answer` + `/webhooks/voice/events`, ~84 lines across `numbers.ts`/`manager.ts`/`.env.example`) — was never ported.** Preserved verbatim at `patches/190bafa-voice-webhooks-scaffold.patch`. It will NOT apply cleanly (`numbers.ts` has since gained per-alias rate limiting etc.) — treat it as a reference for a manual port when voice for ghost numbers is picked up, per the repo rule that Replit-lineage changes are manual ports, never merges. Everything else in the duplicate (409-commit Replit `main`, `subrepl-*` branches, tool caches) was lineage noise with no unique work. |
| Local device-build pipeline established (build 99 on the GHOSTFACE iPhone 14) | ✅ 25 Aug | First working local install since the toolchain saga: Release build from the pre-WIP tree, signed with the local Apple Development cert, installed via `devicectl`, verified on-device as 1.0.2 (99). **Five failures on the way, each now a known fix:** (1) Xcode script phases run with a scrubbed PATH and nvm's node is invisible — `ios/.xcode.env.local` with `NODE_BINARY=$(which node)` fixes the 'Bundle React Native code' phase; it is gitignored and machine-specific, so every fresh prebuild needs it recreated. (2) **Never build from the scratchpad**: macOS `/tmp`→`/private/tmp` symlinking makes Metro see the entry file as outside its project root ('Unable to resolve module …index.js'). Worktree lives at `~/Projects/.ghostface-devtest` (kept, working `ios/` inside). (3) Expo hardcodes `CFBundleVersion` in Info.plist, so `CURRENT_PROJECT_VERSION=99` on the command line is ignored — `plutil -replace` the plist. Needed because installing under 63 is a downgrade iOS refuses. (4) The iPhone 14 (UDID `00008110-000539C61A30A01E`) was not a registered dev device and `xcodebuild -allowProvisioningUpdates` would not register it even with a valid API key — registered directly via the ASC API (`POST /v1/devices`, device id `6Z6X5CK26N`, hand-minted ES256 JWT via openssl). (5) **Five of the six ASC API keys in `~/Keys` are dead** (2P4RFTS6QP, 3L677UK48X, 4D25PA99CF, VSP5B88NFM, VW97H3FBA9 all reject auth); only **WD424K32M4** works. The dead `.p8`s should be deleted and their portal entries revoked if still listed. Also seen: two Expo-added device registrations sit DISABLED on the portal. ⚠️ Reminder: a *device* dev build over the TestFlight install replaces the app but keychain identity survives — confirmed GHOSTFACE stayed signed in. | ⓘ 27 Aug: **the GHOSTFACE iPhone 14 is no longer on local build 99** — reloaded to TestFlight build 63 to check whether it would pick up 64. It will not: 64 was built but never submitted, so 63 is the only build in TestFlight. GHOST remains the sideloaded device.
| ✅ Field test 25 Aug: calls E2E on real devices (build 99 ↔ build 63) | ✅ voice+video verified; 2 wake quirks logged | First full call test since the backplane, the coturn failover, and the hangup fixes — GHOSTFACE (iPhone 14, local build 99) ↔ LITTY (partner's 17 Air, build 63). **Working:** voice call end to end; video end to end with **great quality**; the previously-reported in-video speaker issue is **fixed**; hangup now **tears down both ends** (the class of bug behind the call-dedup work — observed working in live calls; the specific hangup-*during-ring* repro from the GF call-bug row still deserves its own deliberate re-test). Cross-version wire compat 99↔63 confirmed in passing. **Quirk 1 — callee wakes only on the SECOND push:** the first VoIP wake push does not bring the receiving device up; the retry does. Suspects: `CALL_WAKE_GRACE_MS`/`CALL_WAKE_POLL_MS` timing vs how long a cold CallKit wake actually takes, or the first push landing while the socket-kick/reconnect dance is mid-flight. Server logs around a repro would separate push-not-delivered from push-delivered-but-app-not-ready. **Quirk 2 — video call wake 'being funny':** video-mode wake was erratic before connecting (then fine). Same investigation likely covers both. Neither blocks anything; both are pre-launch polish. **Also verified in the same session:** profile photo upload works end to end (the e226d15 feature's first on-device confirmation), and the glass restyle/new graphics look **amazing on real hardware** — the GF-12 "needs device smoke test" caveat on the restyle is now satisfied by build 99. |
| Auth helper duplication — one `lib/auth.ts` | ✅ 27 Aug | Was 7 verbatim `hashToken` copies, 3 divergent `getAuthedAlias`, 3 `requireDeviceAuth`, plus `ws/manager`'s `validateToken`. Now one definition each of `hashToken`, `verifyDeviceToken`, `getAuthedAlias`, `checkAuth`, `deviceAuthMiddleware`; no route defines its own. Landed as three bisectable commits — `7107630` (hashToken, proven byte-identical first), `e4b6213` (resolvers), `1268467` (middleware). Tests 53 → 78. |
| ⚠️ Alias-source confusion hazard — closed by construction | ✅ 27 Aug | The resolvers disagreed on where the caller's claimed alias came from (`?alias=` only vs query-or-body). If a handler read `req.query.alias` while auth resolved from `req.body.alias`, a request would authenticate as one user and act on another. **Audited: never reachable** — all 10 call sites use the alias the resolver returns and none re-read the raw field. `AliasSource` is now a **required** parameter with no default, so omitting it is a compile error and no new route can inherit a wider source by accident. Tested: `"query"` does not fall through to `body.alias` or `body.ownerAlias`. |
| VPN auth failure gate — semantics made explicit | ✅ 27 Aug | `vpn.ts`'s IP gate is consulted *before* the token lookup and charged on the 401/403 paths but **not** the 400: a malformed `:userId` is a client bug, not an auth attempt, and must not fill a bucket that gates real users behind carrier NAT. That was implicit in the old copy; it is now a documented option on `deviceAuthMiddleware({ failureGate })` with its own test. |
| ⚠️ Client auth call sites unproven on device | ⬜ **needs a device** | `uploadEncryptedBlob`, `fetchIceConfig` and `registerInviteOnServer` send the new header but are covered by typecheck only — the app suite does not exercise them. `POST /invites`' route wiring is also untested (routes/invites.ts cannot be imported without a database; its gate is covered in `auth.test.ts`). Exercise all four before flipping `ENFORCE_ENDPOINT_AUTH`. |
| APNs `BadDeviceToken` on call-wake to GHOST — **explained, not a bug** | ✅ closed 27 Aug | Prod logs 28 Aug 00:15 UTC: `APNs VoIP push rejected {"reason":"BadDeviceToken"}` → `Call-wake VoIP push failed to send` (alias GHOST) → Expo push fallback, which delivered. **Cause: GHOST is a sideloaded build.** A locally-signed install registers its VoIP token in the *development* APNs environment while the server pushes to production, so APNs rejects it. **Confirmed by the contrast in the same window:** the VoIP push to GHOSTFACE *succeeded*, because GHOSTFACE had been reloaded to **TestFlight build 63** — ASC-signed, so a valid production token. Both aliases are Benji's own devices; the difference is signing, not hardware. **User-visible consequence:** the Expo fallback arrives as an ordinary message notification, so it does not wake CallKit and a call presents as an alert rather than ringing. Server behaviour is correct throughout. Do not chase as a server or push-routing defect — on a sideloaded build this is expected, and CallKit/VoIP can only be judged on an ASC-signed install. |
| ⚠️ **A failed recovery destroyed the device's existing identity** | ✅ fixed 27 Aug | `recoverIdentity` (`AppContext.tsx`) deleted the device token and all IK/SPK/PQKEM keys **before** calling `/reclaim/verify`, and returned on failure without restoring them. So a mistyped alias, or a phrase for an identity that no longer exists, took a device from "I want my identity here" to "I have no identity at all". Found live: Benji's iPhone 14 was authenticated and ringing via CallKit at 00:28Z, then ended up on the register page after a failed reclaim. **The clear was also a no-op on success** — all seven keys are unconditionally overwritten by the `secureSet` calls straight after — so it only ever had an effect on the failure path, which is exactly where it must not. Fixed by proving possession with the server first and only then writing local state; a failed attempt now leaves the device untouched. tsc clean, app suite 112/112. **Not device-verified** — needs a real failed-then-successful recovery on hardware. |
| Same delete-before-verify hazard in `setAlias` | ⬜ **decide** | `setAlias` clears the same seven keys before `registerWithServer`, and its `else if (reg.conflict)` branch is reached *after* the clear — so a registration that 409s on an already-taken alias also leaves the device with nothing. The intent differs from the recovery case (setting a NEW alias deliberately abandons the old identity, and the comment says so), which is why it was not changed with the recovery fix. But the conflict path specifically destroys an identity in exchange for nothing. Worth the same treatment: register first, write local state second. |
| Recovery-phrase crypto verified end to end | ✅ 27 Aug | Suspected as the cause of a failing reclaim; it is not. 500 random identities generated the way the app does (`randomBytes(32)` → `x25519.getPublicKey`), encoded to a 24-word BIP39 phrase and decoded back: **500/500 round-tripped to the exact byte, and 500/500 reclaim DH proofs verified** against the server's algorithm. A phrase that fails `/reclaim/verify` is a phrase for a different identity, not a broken encoding. |
| ~~Build-72 gate "DB prekey check" was stale~~ | ✅ resolved 27 Aug | Re-run against the current population: **9/9 pass**. See the gate row above. |
| EAS iOS builds fail: `no such module 'WireGuardKit'` | ✅ 28 Aug — **PROVEN on build `d176c63d`** (dev-client profile) | A `development:device` build got past credentials (those are now set up) and **failed in Xcode** with `no such module 'WireGuardKit'` (build `f7e3d00b-2396-4179-b3d4-bf7ddf31f0ca`, a paid build — the earlier non-interactive attempt failed before queueing and cost nothing). **Root cause:** `scripts/link-wireguard-kit.mjs` is documented as a manual post-prebuild step (`expo prebuild && node scripts/link-wireguard-kit.mjs`) and was referenced nowhere in `package.json` or any EAS hook, so on EAS the SPM package was never linked. This affects **every** EAS iOS build once the tunnel target is in the graph, not just dev-client. **Wired 27 Aug:** `eas-build-post-install` runs the link script, and `eas-build-pre-install` runs new `scripts/eas-install-go.sh` — because STATUS already records that **EAS images have no Go toolchain** and WireGuardKit's `wireguard-go-bridge` cannot build without one. Also hardened the generated Run Script phase: the SPM checkout is now located by fallback search (EAS uses a custom derived-data path), `make` is resolved from PATH rather than a hardcoded `/Applications/Xcode.app` path (EAS uses versioned `Xcode_*.app`), Homebrew prefixes are added to PATH, and a missing Go now fails with an explanatory message. ⚠️ ~~All of this is reasoned, not tested — proving it costs another paid build.~~ **28 Aug: PROVEN.** EAS build `d176c63d-de45-4b2a-9099-cabdf82b23f9`, from commit `b63b233`, finished clean — status FINISHED, no error, 5.9 min build, v1.0.2 build 66. The `eas-build-post-install` link step and the `eas-build-pre-install` → `scripts/eas-install-go.sh` hook both ran, and `no such module 'WireGuardKit'` is gone. ⚠️ **The profile was `development:device`, NOT production** — this proves the hooks run and WireGuardKit links on EAS; it does not prove a store build passes, so re-check on the first production build that carries the tunnel target. ⚠️ **Separate lesson — the watcher lied for 5 hours.** The poll loop watching this build ran `eas build:view <id> --json --non-interactive`, but `--non-interactive` is **not a valid flag for `build:view`**: every poll errored, the parsed status came back empty, and the `until` loop never matched FINISHED. It spun for 5h on a build that had already succeeded in 5.9 min. Drop the flag when polling `build:view`. |
| ⚠️ CORRECTION: `development:device` was not missing its API domain | ✅ 27 Aug | `eas.json` gained an `env` block for `EXPO_PUBLIC_DOMAIN` on the grounds that `.env` is gitignored so the build would have had it undefined. **That justification was wrong** — the build output shows EAS already supplies `EXPO_PUBLIC_DOMAIN` from its own "development" environment. The change is kept because an explicit in-repo profile value beats an invisible dashboard setting and now takes precedence, but it did not fix a break. Commit `7c55c72`'s message overstates this. |
| ⚠️ **App Store metadata pointed at a domain we do not own** | ✅ fixed 27 Aug — **`support@` now exists; see the caveats** | `app.json` `extra` declared `privacyPolicyUrl`, `marketingUrl` and `supportUrl` on **ghostface.app**, which returns **530** (registered, Cloudflare NS, but the site is down and it has **no MX at all**, so `support@ghostface.app` bounces). The working site is **ghostface.co.nz** — `/`, `/privacy` and `/support` all return 200. **A dead privacy-policy URL fails App Store review on its own**, independently of GF-01, and TRACKER already recorded the privacy/support pages as "live" — they are, just on the other domain. All three now point at ghostface.co.nz. ⚠️ **`support@ghostface.co.nz` does not exist yet** — create it before submitting or the support URL is dead again (see the row below). ⚠️ **27 Aug, Benji: ghostface.app is NOT ours — it is on backorder.** So this was not merely a dead link: the App Store privacy-policy and support contact pointed at third-party property. If the backorder is caught by someone else, those URLs resolve to a stranger's site under our app's name. Do not point store metadata at ghostface.app again unless and until the backorder actually completes and the domain is under our control. **✅ 28 Aug — the "needs support@ created" blocker is closed, verified independently of the mailboxes row.** `app.json` carries no `ghostface.app` URL in its metadata block: `privacyPolicyUrl` and `marketingUrl` point at ghostface.co.nz and `supportUrl` is `mailto:support@ghostface.co.nz`. All three pages return **200** (`/`, `/privacy`, `/support`). The domain's MX is `1 smtp.google.com`, SPF `v=spf1 include:_spf.google.com ~all`, and the delivery test to `support@` sits in benjamin@'s mailbox dated **2026-08-28 04:19 UTC** (the mailboxes row says 27 Aug — NZT/UTC skew), with **no DSN, bounce or "Address not found" anywhere in the following 14 days.** ⚠️ **The mailboxes row's caveat is still live and now has evidence behind it:** that thread holds exactly one message, labelled `SENT` only — no `INBOX` copy came back, so benjamin@ is not a member of whatever `support@` is. No-bounce against a Google MX is good evidence the address exists, but nobody has opened the inbox. **Eyeball it once before submitting.** ⚠️ **Separate and unresolved: `supportUrl` is a `mailto:`, not a URL.** App Store Connect's Support URL field expects http/https, and `https://ghostface.co.nz/support` already returns 200 — decide which value goes into the ASC listing before submitting. 🔧 **Row structure repaired 28 Aug:** this row had four cells in a three-column table, so everything from "⚠️ 27 Aug, Benji:" onward — the fact that ghostface.app is on backorder and not ours — was being dropped at render and was invisible on the board. Merged into this cell; no text lost. |
| Company mailboxes on ghostface.co.nz | ✅ 27 Aug — 5 addresses, all accepting mail | Mail for the domain is **Google Workspace**, not iCloud: MX `1 smtp.google.com`, SPF `v=spf1 include:_spf.google.com ~all`. Live: `benjamin@` (the connected Gmail account), plus `support@`, `info@`, `legal@` and `admin@`, all created 27 Aug. Delivery tests sent to all four from benjamin@ — **no bounce returned for any**, which is solid evidence they accept mail. Caveat kept deliberately: if an address is a Group without benjamin@ as a member, a successful delivery is indistinguishable from silence at the sending account, so eyeball each inbox once rather than treating no-bounce as proof. `support@` gates the App Store support URL in `app.json`, so that blocker is now closed. ⚠️ A password for one of these was pasted into a session transcript on 27 Aug and must be rotated. |
| ⚠️ **SECURITY: a ghostface.co.nz mailbox password was pasted into a session transcript** | ⬜ **not started — rotate** | Recorded 27 Aug as a trailing sentence on the mailboxes row above, where it was easy to miss; promoted to its own row 28 Aug. A password for one of the five ghostface.co.nz addresses (`benjamin@`, `support@`, `info@`, `legal@`, `admin@`) was pasted into a session transcript on 27 Aug. Not public, but a live credential somewhere it should not be — and these are Google Workspace accounts, so the blast radius is mail for the company domain, including `legal@` (the MinterEllison correspondence that GF-01 turns on) and `support@` (the App Store support contact). **Same class of mistake as the WireGuard private key on 24 Aug**, which was caught and rotated the same day; the lesson recorded on that row — keep secrets out of anything that lands in a transcript — applies identically. **Action: identify which of the five it was, rotate it in the Google Workspace admin console, and review that account's sign-in activity for anything unexpected.** Rotating is cheap and the exposure window has been open since 27 Aug. |
| ⚠️ CORRECTION: company email is Google Workspace, not iCloud | ✅ 27 Aug | STATUS's "Company email setup — DONE (19 Aug): iCloud Custom Email Domain for ghostface.co.nz" is **stale**. Live DNS shows MX pointing solely at `smtp.google.com` — Workspace took the domain over around 23 Aug (there is a Workspace setup email in the inbox dated 23 Aug). Also corrects an in-session worry of mine that two mail systems were both claiming benjamin@ghostface.co.nz: they are not. The Vonage correspondence was simply never addressed to the domain at all — it went to Benji's personal mayyybachhh@icloud.com. |
| Email authentication on ghostface.co.nz (SPF / DKIM / DMARC) | ✅ 27 Aug — **verified passing end to end** | DNS at Vercel (`ns1/ns2.vercel-dns.com`, team `ghostfacecommunications`). **SPF** `v=spf1 include:_spf.google.com ~all`. **DKIM** `google._domainkey` (`rec_c7006f1ba95aaa7d20f829a6`), 2048-bit, selector `google`, enabled in Workspace 27 Aug. **DMARC** `_dmarc` (`rec_a71d7c277b12aa610896ff5f`) `v=DMARC1; p=none; rua=mailto:admin@ghostface.co.nz; fo=1`. **Verified by an external authentication check, not by assumption:** an earlier self-addressed test was inconclusive because mail injected via the Gmail API never traverses the outbound signing path — a real send to a third-party verifier returned `SPF pass` / `DKIM pass (header.d=ghostface.co.nz, s=google, 2048 bits)` / `iprev pass`, with a genuine `DKIM-Signature` on the wire. Both SPF and DKIM align with the From domain, so DMARC passes on both. **Next step (not urgent):** let `rua` reports accumulate at admin@ for a week or two to catch any unknown sender, then tighten `p=none` → `p=quarantine`. |
| Invite deep links — `ghostface.co.nz/i/<CODE>` | ⬜ spec'd 27 Aug, not implemented | Spec at `artifacts/ghostface/docs/INVITE_DEEP_LINK.md`. Addresses the cold-start problem: with no contact discovery by design, an invite code is the **only** route to a first conversation, and it currently has to be retyped out of a chat message. Foundations already exist — `scheme: "ghostface"` is declared, the landing site is live static HTML on Vercel with `/privacy` and `/support` as the same directory pattern, and `lookupInviteCode`/`consumeInviteCode` already implement lookup-then-consume. Missing: `ios.associatedDomains`, Android `intentFilters`, the `/i/` page and an in-app route. ⚠️ **A deep link cannot survive an App Store install** — the only things that achieve it are fingerprinting SDKs (Branch/AppsFlyer), which the spec rejects outright as incompatible with the product, and silent clipboard reading, rejected on optics. So the ceiling is: one tap if installed, one deliberate paste if not. Phase 1 (landing page + an onboarding "have an invite code?" paste field) needs no native change and carries most of the value. |
| ⚠️ **Onboarding promises a prize that is never granted** | ⬜ **decide before launch** | Onboarding tells every new user they have WON a free month and the server grants nothing — there is no welcome-gift code at all; the only writer of `ghost_entitlements` is the Solana/USDC payment path in `routes/crypto.ts`. The comment in `app/onboarding.tsx` claiming the grant and a one-per-account cap are "enforced server-side" is false. Two further inaccuracies: the odds are **100%** (`WELCOME_PRIZES[floor(random*3)]`, no losing outcome) while the copy says "a chance to win", and the **client** picks the prize — if it ever tells the server what it won, anyone can claim the best one. Design at `artifacts/api-server/docs/WELCOME_GIFT.md`. |
| Welcome gift: guaranteed gift vs prize draw | ⬜ **question for counsel** | A guaranteed gift needs no odds disclosure and no promotional-competition rules. A genuine draw brings in Apple's sweepstakes requirements (developer as sponsor, published official rules, legally permitted in every shipped territory — and the stated intent is worldwide) and NZ promotional-competition rules under the Gambling Act. Cheap to add to the live MinterEllison thread now, expensive to get wrong after launch. **Recommendation: guaranteed gift**, and do not ship "chance to win" wording until that is settled. |
| GHOST NUMBER must not be in the prize pool | ⬜ | It is a recurring cash cost (rented Vonage number + SMS, monthly, per user), it is not an entitlement `plan` so it has no representation in `ghost_entitlements`, and it is **not yet known to be permitted** — whether numbers may be assigned to individual end users is question 1 of the unsent-until-27-Aug Vonage email. Granting it would be an unfunded liability on a model the supplier has not approved. The client's prize table should lose its third entry. |
| ✅ Welcome gift VERIFIED end to end on hardware | ✅ 28 Aug | First gift ever granted: identity `MAYYBACHHFKU` registered 16:13, `welcome_gifts` row `specter` at 16:14, `ghost_entitlements` `specter` until 2026-09-27. The whole chain fired — sign-up → `claimWelcomeGift()` → authenticated endpoint → idempotency row + entitlement in one transaction. Both tables were empty before, so this is also the first entitlement of any kind: **nobody has ever paid**, which makes early monitoring easy (any entitlement before a payment is the trial working). ⬜ Still untested: the second call reporting `alreadyClaimed` rather than granting twice. |
| ✅ Self-destruct VERIFIED releasing an alias | ✅ 28 Aug | `G_FACE` was wiped from the device and vanished from `identity_keys` — the sanctioned `DELETE /api/prekeys/:userId` path, which proves ownership with the device token. First time verified. This is the path that was NOT available for GHOSTFACE (keys already lost), which is why that one needed a manual database deletion. |
| ⚠️ First gift attempt failed — wrong build, not a bug | ✅ explained 28 Aug | `G_FACE` registered but no gift appeared. Cause found in the HTTP logs' User-Agent strings: the sign-up came from **GHOSTFACE/66**, the 25 Aug production build, which predates `claimWelcomeGift()` entirely. The only `POST /api/welcome-gift` in the whole log was a `curl` 401 test. Several builds (61/63/66/120) share the bundle id across the devices, so it is easy to sign up in the wrong one. **Build 120 is the dev build — identify it by the coin spinning on its own.** |
| ⚠️ Language setting offered 12 languages and did nothing | ✅ cut to English 28 Aug (`b16bce0`) | Settings listed en/es/fr/de/ja/zh/ar/pt/ru/ko/hi/it with flags and native labels, validated and persisted — but there is **no i18n library, no translation files and no translation call sites**; every string is a hardcoded English literal. Picking ESPAÑOL stored `"es"` and changed nothing, which reads as a broken app and is functionality advertised but not delivered (App Store risk independent of GF-01). Also de-duplicated `VALID_LANGUAGES`, which existed twice in AppContext; narrowing it to `["en"]` migrates anyone who had picked another language. **Restore only alongside real translations — Arabic last, it needs RTL layout, not just strings.** |
| Provisioning profiles: the multi-select trap | ✅ resolved 28 Aug | Four builds failed to install on the iPhone 14 with `0xe8008012`. Registering a device with `eas device:create` does **not** add it to an existing profile, and a `--non-interactive` build silently reuses the profile it has. Worse, the device list in `eas credentials` is an inquirer checkbox: **Enter submits only what is ticked, and nothing is ticked by default**, so pressing Enter on the highlighted row provisions exactly one device. Press **`a`** to toggle all. Both targets need it — `com.ghostface.app` AND `com.ghostface.app.tunnel`. Diagnose by dumping the profile from inside the ipa rather than inferring from install errors: `unzip -p <ipa> 'Payload/*.app/embedded.mobileprovision' \| security cms -D`. |
| Dev builds installed on all three handsets | ✅ 28 Aug | 12 Pro, iPhone 14 and iPhone Air all running the current branch. The Air needed `xcrun devicectl manage pair` — being plugged in and unlocked was not enough, it had never been paired with this Mac, which surfaced as "device not able to fulfill the requested usage assertion" rather than anything about pairing. |
| ⚠️ Dev client not reaching Metro | ⬜ **blocks visual iteration** | The phones run their embedded bundles, so JS-only changes (glow, copy, Pig Latin removal, scroll fix) are committed but invisible on device. Most likely iOS **Local Network permission** — check Settings → GHOSTFACE → Local Network, and test `http://<mac-ip>:8081` in Safari on the phone: loads → permission; fails → wrong network. Until this works every visual tweak costs a paid build. |

## Incidents

| Incident | Status |
|---|---|
| **VPN peer agent down + box unfirewalled (24 Aug)** | ✅ **CLOSED 24 Aug** — all three steps of the recorded fix order done and the control plane verified end to end for the first time since it was built. (1) **Unwedged** — dropped the stalling host (`129.213.151.234`) *before* restarting, not after; new pid, `Recv-Q` back to 0, CLOSE-WAIT sockets gone, localhost answering in 5ms. (2) **Firewalled** — `infra/vpn-agent/firewall.sh` (`8672bf3`) restricts tcp/8443 to the api-server’s three Railway static outbound IPs plus localhost via its own `VPNAGENT` chain; SSH/WireGuard/INPUT policy untouched, persisted by `vpn-agent-firewall.service`. Verified from an outside IP: 8443 times out, SSH/api/coturn unaffected. (3) **Rewritten** — `infra/vpn-agent/agent.py` (`9a68f07`), now in-repo: TLS off the accept loop (the root cause), threaded, backlog 5→128, handshake/request/`wg` timeouts, bounded Content-Length, `GET /healthz`; the original 7-stalled-connection attack reproduced against prod with `/healthz` answering throughout. Two latent bugs fixed while testing: `remove_peer_from_conf` corrupted `wg0.conf` from the second peer onward, and the read-modify-write is now serialised under a lock with atomic (`os.replace`) writes. End-to-end verified via the app’s own API with throwaway alias `ZZVPNTEST1` (register → 200 in 1.15s, `tunnelIp 10.66.0.2`; GET persisted; DELETE removed), which also proves the egress allowlist is correct. ⚠️ **Standing coupling:** Railway reassigns static outbound IPs when a service changes region — another move breaks peer registration silently and will look exactly like this incident; re-run `firewall.sh` with fresh values from `railway outbound-network static-ip status --service api-server`. ⚠️ **Still true: the end-to-end tunnel is NOT achieved** — the control plane works, the *client* does not exist yet (see the WireGuard native-client row above). **Do not cancel or downgrade Mullvad.** Full diagnosis kept in STATUS.md. |
| Postgres password rotation (19 Aug) | ✅ closed — zero-outage second rotation; credential canonical in secrets-store (`postgres-ha-cluster-password` v1). Runbook in STATUS.md. |
